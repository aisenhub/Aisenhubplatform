# Agent 工作规则

## 安装软件要求

1. 支持自定义路径的软件统一安装到 `D:\APP\Codex`，不要把该目录当作单个软件的根目录，要在该目录下创建对应软件的文件夹再安装到里面。
2. 不得主动安装到 `C:\` 或默认路径，该情况下请告知。

已有工具优先复用；项目依赖安装到项目约定目录。需要另装系统软件时先检查上述路径条件；不能为了完成任务自行改用C盘默认安装。

## 阅读与边界

先阅读 docs/README.md、docs/architecture/overview.md、docs/agents.md 和 [Agent 开发与发布流程](docs/guides/development-release-workflow.md)，再阅读当前任务相关模块、reference 合同、对应 proposal 及实际源码。开发前必须按该流程标注 R0/R1/R2/R3、受影响消费者、本地验证、生产门槛。architecture 维护当前实现，proposals 维护优化设计、总计划和分阶段计划；计划存在不代表已实现或已授权执行。任务范围以用户当前派发为准。用户指令优先于本文件；公共合同冲突须先核对代码并同步修订，不在局部代码静默选择另一套规则。

只执行已派发的任务及必要依赖检查；任务清单的存在不等于获准自动开发全部模块。不要自行增加组织、支付、微服务或第三方接入能力。依赖门槛未通过时完成独立准备，报告具体阻塞，不伪造通过结果。

Supabase操作先核对当前官方文档及固定版本；遵循可用技能要求。共享领域过程是业务规则唯一写入口，Admin、Account API和后台任务不得各自实现权益/配额算法。

新平台接入，或维护SDK、Auth适配、BFF、公共合同、Registry、环境配置与参考页面时，必须阅读[新平台接入手册](docs/guides/platform-onboarding.md)，检查并同步受影响的参考代码、配置、用户待办及验收步骤。手册必须与实际可安装包和接口一致，不把未发布能力写成现成用法。

## 工程和测试

明确当前任务ID、依赖、变更目录、合同和验收条件。公共OpenAPI/类型/迁移改动需要同步消费者和测试；不随意重命名跨模块字段。迁移由固定版本CLI生成时间戳，不能修改已应用生产迁移来掩盖问题。

完成对应权限、并发或失败恢复测试；运行尚未存在的命令不能算测试。测试报告区分静态检查、Local（含适用的本地 Supabase）、CI、Provider 和生产观察，结果仅使用 PASS/FAIL/NOT_RUN/PARTIAL/BLOCKED，不适用另写理由。R1 要求本地检查和精简生产门槛；R2/R3 另须完成适用的本地 Supabase 验证及相应生产门槛。修复必须保留失败用例，不能删测试或放宽授权以通过CI。

记录必要、脱敏的证据；不提交真实Token、Key、SQL密码、用户数据、OAuth code、MFA secret、兑换码或配置文件内容。示例凭据必须明显虚构。

## 交接与 GitHub 阶段同步

单人单机默认一次一个任务，使用main与短期codex/任务分支，不要求长期develop分支。用户已长期授权Agent对明确派发的任务创建分支、小提交、推送，并在[开发与发布流程](docs/guides/development-release-workflow.md)第2.2节全部条件满足后自动合并main及推送，无须逐次询问。目标为https://github.com/aisenhub/Aisenhubplatform.git；推送后核对远端commit，不把本地commit当作已上传。

先检查分支、远端main与工作区，不强推、不覆盖用户工作；只暂存本任务明确文件，提交前检查差异及敏感信息。R0/R1满足适用检查和门槛条件后可合并，R2/R3必须完成适用Local/CI、本地 Supabase及领域验收。main更新或解决冲突后重验；必需检查失败、未决业务规则或归属不明时保留分支并处理阻塞。若main合并/推送会自动部署生产，须先完成生产门槛并已有生产授权；绑定未知不得擅自合并。生产部署、真实支付、费用变更与破坏性操作不在Git长期授权内，已有明确授权不重复询问。

任务结束报告：完成内容、变更文件、执行验证及结果、未完成/阻塞、commit与push状态、下一项满足依赖的任务。不把NOT_RUN写成PASS，不把“文档已规划”写成“功能已实现”。
