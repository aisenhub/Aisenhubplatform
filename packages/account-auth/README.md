# @kit/account-auth

Provider-neutral authentication contracts and session/proof validation helpers.

This repository currently produces private local tarballs for M5 validation. It
is not published to npm or a public Registry. Use the package root, `/browser`,
or `/server` export; all three are runtime-neutral for this package.
