# @kit/account-server

Server-only typed client and authorization helpers for the central Account API.
The `/server` export is explicit; the package contains no browser entrypoint.

This repository currently produces private local tarballs for M5 validation. It
is not published to npm or a public Registry.
