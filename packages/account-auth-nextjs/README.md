# @kit/account-auth-nextjs

Next.js and Supabase authentication adapters with explicit browser and server
exports. Import browser-safe client creation from `/browser`; use cookies,
refresh, PKCE, and request-scoped server helpers from `/server`.

This repository currently produces private local tarballs for M5 validation. It
is not published to npm or a public Registry.
