# 开发规划复核记录

## DP2复核（2026-09-08）

本轮授权范围：进度校准、T12/T16收尾细化、M4第三批以及M5/M6后续路线。仅修改文档，不执行应用/数据库/云端变更。

- 将README、总计划、验证入口与模块状态中的过时当前描述更新；旧证据保留历史输出并补后续进展引用。
- T13～T15 Local SQL/SDK与完整HTTP/UI范围分开；T18-L/T18-S分层验收，M3-R1核对完整G3情景。
- 新增10项收尾任务、11项M4任务、M5/M6各6项；每项有依赖、范围、验收和外部条件。
- 核对Admin proof已在OpenAPI内，普通proof流程仍需补齐；登记logout范围、网关配置及SQL独立角色/TLS的未闭环差异，不在规划中静默认可替代合同。
- M4的备份屏障基础与M6正式备份分工明确；M5/M6不等M4结束再开始设计，按Gate冻结细节。
- 文档分支从已推送应用基线派生，包含25个尚未合入main的实现提交；本次不合并main或部署。

本轮静态验证：`node tooling/scripts/src/docs-check.mjs`通过（文档链接、首批18项依赖及验收ID）；`node tooling/scripts/src/openapi-check.mjs`通过（当前17个Account/32个Admin操作结构，非运行覆盖）；Node临时只读检查33项新增任务的依赖存在、无环与导航/正文一致性通过；`git diff --check`通过。最初两处验收ID缩写检查失败，已改为完整编号后复验通过。

定向执行`pnpm exec oxfmt --check README.md docs/development`返回无匹配文件；项目`.oxfmtrc.jsonc`明确排除`*.md`，因此不记录格式化器PASS。Markdown围栏/链接/任务结构另行静态检查。仅文档变更，本轮未运行应用构建、SQL、浏览器或Staging验收。提交与远端SHA在本任务最终交接报告中记录；基线及限制见[DP2证据](evidence/DP2-baseline.md)。

最终变更范围为30份Markdown；围栏闭合检查通过，有限模式扫描未发现JWT、Supabase/GitHub Secret、私钥或带密码SQL连接串。扫描不宣称覆盖所有凭据形式；本轮未读取真实环境配置内容。

## DP1历史复核（2026-09-07）

复核日期：2026-09-07。范围为架构v1.2到开发规划的可追溯性、七模块边界、首批任务依赖和验收定义。本记录是文档审核，不是应用验收报告。

## 已修正

| 问题 | 修改结果 |
|---|---|
| 公共合同的Account接口数量与API表不一致 | 统一为17个方法/路径组合；T11逐条生成OpenAPI；近期认证若增加接口须同步基线 |
| 资料并发版本只在实施规格中出现 | Profile/Preferences的row_version同步写回数据模型，成功条件更新才递增 |
| 平台Key响应丢失处理不够明确 | 固定HMAC表示、creation_operation_id及唯一约束；按操作查询元数据、撤销未知交付Key后新建，不恢复明文 |
| 本地安装验收与真实发布混用 | 分为G5-L和G5-P，统一总计划、模块和外部依赖；文件同样区分G4-L/G4-S |
| 删除请求状态命名不一致 | 统一pending_admin/approved/cancelled；批准与门闩/job同事务，清除结果读取job |
| 进度记录落后于GitHub事实 | C1/C2已同步；C3交付文件及同步事实在status单独记录 |

## 静态检查范围

检查相对文件链接、Markdown围栏、M0～M6文件覆盖、T01～T18编号完整性、导航与正文依赖一致性、依赖无环、具体验收ID存在性、API表数量及提交空白错误。检查本轮文档是否包含常见真实凭据格式；这是有限模式检查，不宣称能识别一切秘密。

结果与实际运行摘要见[实际进度](status.md)。SQL片段只作设计输入；本轮未运行PostgreSQL迁移、权限测试或业务测试。

## 尚需实际验证

五项SP探针及全部V用例仍为NOT_RUN。普通用户近期认证协议、Auth最小列授权、托管pooler角色和上传取消语义必须按任务实测；不得将待验证细节当作已经成立的产品能力。

Staging、域名/部署环境、Google/SMTP、备份告警、发布地址和生产容量依次按X01～X06补齐，不阻塞T01的只读核验。M0～M6均未开始实现。

下一步可派发[首批任务T01](tasks/batch-01.md)，完成后按依赖进入T02。七份模块规格已经给出实施范围和验收；M3/M4更细代码任务由T18结合已实现公共合同拆分，避免以不存在的接口作为开工条件。
