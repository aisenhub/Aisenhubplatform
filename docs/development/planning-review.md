# DP1 规划复核记录

复核日期：2026-09-07。范围为架构v1.2到开发规划的可追溯性、七模块边界、首批任务依赖和验收定义。本记录是文档审核，不是应用验收报告。

## 已修正

| 问题 | 修改结果 |
|---|---|
| 公共合同的Account接口数量与API表不一致 | 统一为17个方法/路径组合；T11逐条生成OpenAPI；近期认证若增加接口须同步基线 |
| 资料并发版本只在实施规格中出现 | Profile/Preferences的row_version同步写回数据模型，成功条件更新才递增 |
| 平台Key响应丢失处理不够明确 | 固定HMAC表示、creation_operation_id及唯一约束；按操作查询元数据、撤销未知交付Key后新建，不恢复明文 |
| 本地安装验收与真实发布混用 | 分为G5-L和G5-P，统一总计划、模块和外部依赖；文件同样区分G4-L/G4-S |
| 删除请求状态命名不一致 | 统一pending_admin/approved/cancelled；批准与门闩/job同事务，清除结果读取job |
| 进度记录落后于GitHub事实 | C1/C2已同步；C3交付文件及同步事实在status单独记录 |

## 静态检查范围

检查相对文件链接、Markdown围栏、M0～M6文件覆盖、T01～T18编号完整性、导航与正文依赖一致性、依赖无环、具体验收ID存在性、API表数量及提交空白错误。检查本轮文档是否包含常见真实凭据格式；这是有限模式检查，不宣称能识别一切秘密。

结果与实际运行摘要见[实际进度](status.md)。SQL片段只作设计输入；本轮未运行PostgreSQL迁移、权限测试或业务测试。

## 尚需实际验证

五项SP探针及全部V用例仍为NOT_RUN。普通用户近期认证协议、Auth最小列授权、托管pooler角色和上传取消语义必须按任务实测；不得将待验证细节当作已经成立的产品能力。

Staging、域名/部署环境、Google/SMTP、备份告警、发布地址和生产容量依次按X01～X06补齐，不阻塞T01的只读核验。M0～M6均未开始实现。

下一步可派发[首批任务T01](tasks/batch-01.md)，完成后按依赖进入T02。七份模块规格已经给出实施范围和验收；M3/M4更细代码任务由T18结合已实现公共合同拆分，避免以不存在的接口作为开工条件。
