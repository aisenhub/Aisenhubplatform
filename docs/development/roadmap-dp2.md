# DP2：从当前实现到V1发布候选

更新：2026-09-09。DP2是在架构v1.2、DP1模块规格上的执行规划修订，不改变产品范围；本文件同时记录当前实施进度，不代表已完成托管部署、正式产物发布或生产操作。

## 当前起点与路线

代码基线`31b5142421847c26e61fe733b0df67f2fecd115a`已在远端任务分支；main仍为规划提交`f981ca533db67e543bbe3f6d88c337b78c0199d4`。已有工程、M1基础、M2部分链路及M3 Local实现；G2-L仍需正式收口，G0-S/G2-S尚未完整通过，M4～M6未实施。详情见[校准证据](evidence/DP2-baseline.md)和[进度](status.md)。

| 阶段 | 本次计划精度 | 进入与完成条件 |
|---|---|---|
| 首批收尾 | [逐项可派发规格](tasks/closeout-01.md) | T12/T16闭环→T18-L确认G1/G2-L；托管线→T18-S确认G0-S/G2-S |
| 权益阶段收口 | M3-R1逐用例核对 | 已有Local实现→完整G3证据；不重新开发Ledger |
| M4 | [第三批完整任务](tasks/batch-03.md) | M4-01可先做；G2-L后实施→G4-L；托管线具备后→G4-S |
| M5 | 本文任务、资源与验收清单 | 早期盘点可先做；M4稳定后冻结集成细节→G5-L→获授权后G5-P |
| M6 | 本文任务、外部输入与演练门槛 | 环境/备份协议先准备；对象模型与托管稳定后演练→G6 |
| 生产执行 | 独立发布任务 | G6后取得明确生产发布授权，再执行与观察 |

不按未经验证的工期排日期；以门槛解锁。可独立准备不代表自动并行开agent或执行所有任务。每项依赖任务只列新增任务间依赖；额外Gate/外部输入均须同时满足。

## M5资源覆盖与责任

依据[M5规格](modules/M5-sdk-admin-registry.md)、[API/模板合同](../api-sdk.md)、[公共合同](contracts.md)。T16-R1交付最小平台/Origin/Key/账户管理，M3交付权益资源，M4交付文件和删除流程；M5负责统一、完善和可复用安装，不把基础授权缺失留到界面收尾时绕过。

| 资源 | 前阶段提供 | M5完成 |
|---|---|---|
| 平台/Origin/Key/账户 | T16-R1必要领域/API与动作 | 列表/详情/筛选分页、轮换交付状态、同步漂移、状态操作完整导航 |
| Plan/订阅/批次/Code | M3 Local与M3-R1 | Plan归档/默认Free、命令审计、批次确认交付、code mask/按码禁用及缺失受控入口 |
| 文件/策略/删除任务 | M4-08/M4-09 | unknown/预算/blocked详情、受控retry、下载MFA、恢复引导 |
| 审计 | 各领域事务记录 | Admin只读分页筛选和脱敏详情；缺API时补受控查询，不给表SELECT权限 |
| 用户模板 | T12/T16与M3/M4最小UI | Login/Signup/Forgot/Reset/OAuth、Pricing、Profile/Preferences、Subscription/Redeem、Files、UserMenu |

所有尚缺领域/API能力先记录到覆盖表，再在所属M5任务以独立子提交交付并测试；不得只做好按钮或返回假成功。所有页面覆盖loading/empty/error、无权限、冲突和敏感proof过期；不提供Code明文恢复、Projection直接编辑、任意checkpoint或SQL。

| ID | 交付 | 依赖任务 | 初始状态 |
|---|---|---|---|
| M5-01 | 资源/包/模板差距与兼容矩阵 | 无 | DONE（盘点交付；见 m5-compatibility-matrix.md） |
| M5-02 | 三SDK可安装版本产物 | M5-01,M4-10 | PASS（Local tarball；未发布） |
| M5-03 | Admin资源与错误状态完善 | M5-01,M4-10,M3-R1 | PASS（Local；资源导航、审计、服务端筛选、状态动作及阻塞原因展示） |
| M5-04 | Registry与完整用户模板 | M5-02 | PASS（Local manifest、Auth与用户模板；未正式发布） |
| M5-05 | 全新Consumer实际安装验收 | M5-03,M5-04 | PASS（G5-L Local；Hosted双Origin/Platform待X05） |
| M5-06 | 正式scope/Registry发布与安装 | M5-05 | WAITING（另需X05及发布授权） |

## M5-01 — 差距与兼容矩阵

- 依赖任务：无。
- 范围：`docs/development`、包/Registry清单设计；读取当前代码与OpenAPI，不先改业务实现。
- 交付：按上表逐资源记录领域/HTTP/SDK/UI/测试事实；包exports、依赖、SemVer、API序列化与模板安装覆盖表；记录X05待定scope/host。M4字段未冻结处显式引用M4-01，不猜字段。
- 验收：每一项缺口都有M5-02/03/04等承接任务和测试；前阶段必须条件不转成可忽略项。M4-10完成后复核冻结，计划时READY不代表可提前完成所有产物。

## M5-02 — SDK打包与兼容

- 依赖任务：M5-01,M4-10。
- 范围：三个account包、domain公开类型、打包脚本和合同测试。
- 交付：显式browser/server exports、依赖白名单、可重复tarball、完整Account能力，真实序列化对应OpenAPI；上传不自动重传，业务授权3秒/普通5秒预算及允许重试集合遵循API专题。
- 验收：V-SDK-01/02；错误/request_id/ETag/分页/NULL/free/perpetual往返；浏览器导入server包失败，包内无内部SQL、环境文件或测试凭据；Node/Edge兼容及独立安装类型检查。未正式发布不宣称可从npm获取。Local证据见`evidence/M5-02.md`。

## M5-03 — Admin成品

- 依赖任务：M5-01,M4-10,M3-R1。
- 范围：`apps/admin`、`packages/ui`、必要只读/领域API包装、OpenAPI及浏览器测试。
- 交付：资源表全部页面和动作的导航、分页筛选、状态、原因、确认和失败恢复；平台、Origin、Key、账户和文件列表的查询参数必须由同一受控 Admin SQL wrapper 校验并服务端过滤；MFA过期保留非Secret输入并重验证。Key新建→部署确认→撤旧，Batch一次交付→显式确认，禁止明文重取。
- 验收：V-UI-01和相关V-AUTH-03；每入口服务端授权、Audit、真实拒绝/恢复路径；Admin身份无法激活普通平台；不引入第二套UI/RBAC框架。缺领域API先补同一业务入口再连UI。

## M5-04 — Registry与用户模板

- 依赖任务：M5-02。
- 范围：`registry`、`apps/template-preview`、模板构建/校验、安装说明。
- 交付：API专题列出的全部模板、品牌name/logo/routes、固定CLI版本、SDK兼容区间/精确构建版本/校验和。模板只复制UI、路由和SDK胶水；不复制授权、权益/配额算法。
- 验收：Registry依赖可解析；错误、未激活/停用/关闭、公开Pricing无需登录、Profile412、兑换、文件unknown及删除预算状态正确；构建可复现且Secret扫描通过。preview不作为生产部署物。

## M5-05 — G5-L真实安装与接入

- 依赖任务：M5-03,M5-04。
- 范围：独立Consumer fixture、CI、安装报告、必要兼容修复。
- 验收：G5-L、V-INTEGRATION-01、V-SDK-01/02、V-UI-01；从空Next项目安装本次tarball和Registry，不能用workspace链接，两个不同Origin/Platform完成Auth/刷新/激活/Profile/兑换/文件/停用全链路。只配置而不改核心Domain，无platform硬编码；扫描浏览器与包内容，执行typecheck/build/E2E。
- 交接：可复现安装命令、版本与校验和、Local/托管覆盖边界；固定依赖，缓存依工具隔离，不为测试默认安装系统软件到C盘。

## M5-06 — G5-P正式产物发布

- 依赖任务：M5-05。
- 外部条件：X05正式scope、Registry host及最小发布凭据；明确产物发布授权。
- 范围：版本/变更说明、受控发布流程和正式地址安装验证。
- 验收：G5-P；固定版本可访问、内容校验和与验收产物一致，从正式地址全新安装回归通过。缺权限保持BLOCKED，G5-L结论保留；不得把本计划推送或CI成功视作发布授权。

## M6早期准备与运行门槛

依据[M6规格](modules/M6-operations-release.md)、[运维合同](../operations.md)及[外部输入](decision-register.md)。M6-01在M4-01完成后即可做设计，不等待M5，避免到恢复演练时才发现文件删除与备份屏障不兼容。

| ID | 交付 | 依赖任务 | 初始状态 |
|---|---|---|---|
| M6-01 | 环境/配置/备份与运维协议 | M4-01 | DONE（设计交付；见 m6-operations-protocol.md） |
| M6-02 | 联合备份与外部墓碑实现 | M6-01,M4-10 | WAITING |
| M6-03 | 隔离恢复与轮换演练 | M6-02,M4-11,M3-R1 | WAITING |
| M6-04 | 可观测性、容量和告警演练 | M6-01,M4-11 | WAITING |
| M6-05 | G6发布候选审查 | M6-03,M6-04,M5-06,T18-S | WAITING |
| M6-06 | 获授权的生产发布与观察 | M6-05 | WAITING（单独授权） |

## M6-01 — 环境、配置与备份协议

- 依赖任务：M4-01。
- 范围：环境配置模板、runbook规格、决策/合同记录，不购买服务或启动生产任务。
- 交付：X02/X04/X06和P01～P08确认清单；环境/Secret/角色/Origin/调度/版本漂移矩阵；M4屏障与删除墓碑接口对应联合manifest字段和独立保存策略。区分主库、对象、配置、Secret恢复，不将数据库备份当对象备份。
- 验收：每个资源有责任类别、最小权限、待输入、演练步骤和失败/退出条件；RPO24h/RTO4h仅目标。屏障失败/超2h先判恢复集失败再解除，恢复邮件隔离、未知写入处理和删除墓碑重应用都有承接任务。

## M6-02 — 联合备份与墓碑持久化

- 依赖任务：M6-01,M4-10。
- 外部条件：X04独立备份、加密和凭据托管；缺少时仅做隔离模拟，不宣称真实备份完成。
- 范围：持久备份worker、配置/manifest、M4屏障消费、受限外部墓碑保存、恢复集校验。
- 验收：V-OPS-01备份子集；每日DB一致快照与不可变对象清单、size/hash、配置/迁移版本、完整标记；缺active对象/复制失败不能成功，恢复集失败后才解除屏障，新增上传不阻塞。外部墓碑与主库独立，Secret不随普通dump明文混放。

## M6-03 — 隔离恢复与凭据轮换

- 依赖任务：M6-02,M4-11,M3-R1。
- 外部条件：隔离恢复目标及受控操作授权，P06/P07确认；不以生产作为首次恢复实验。
- 范围：恢复/轮换脚本、固定工具runbook、实测证据，必要适配修复。
- 验收：V-OPS-01/02；DB+对象恢复与hash、外部墓碑重应用；恢复出的session/Platform Key/未用Code失效；HMAC新旧verify-only、SQL/Storage/Auth/备份凭据分用途轮换。管理员限时恢复原子替换singleton、撤旧session/proof并关闭入口。
- 记录：RPO/RTO实际值、丢失窗口、失败重试、unknown/deleting/lease恢复；只有独立可信事件才能按operation_id补录，不猜Ledger。runbook须有已验证命令和权限，不以概念清单通过。

## M6-04 — 容量、监控与告警

- 依赖任务：M6-01,M4-11。
- 外部条件：X04告警接收渠道、X06容量/预算、P04/P05/P08确认。
- 范围：指标、受控压测、告警、runbook及阈值配置。
- 验收：V-OPS-03；候选100rps/15min、p95≤500ms、错误率<1%按确认目标实测，记录资源成本；同时覆盖上传内存、DB连接/锁、兑换冲突、unknown预算、删除积压、lease、备份年龄/失败。告警实际送达测试渠道；不缓存授权或释放unknown换取通过。

## M6-05 — G6发布候选

- 依赖任务：M6-03,M6-04,M5-06,T18-S。
- 范围：发布候选证据、兼容/迁移/回退清单、CI和PR集成审查。
- 验收：G0-S/G1/G2-S/G3/G4-S/G5-L/G5-P及V-OPS-01/02/03完整；X01～X06与相关默认值确认，候选代码/配置/迁移固定，备份恢复/轮换/告警有报告，无未闭环安全P1或未知数据丢失路径。核对远端及main集成，不自动合并。
- 交接：expand→兼容部署→验证→contract顺序、forward-fix及回退阈值、观察窗口和操作责任；G6 PASS只产生发布候选，不等于生产已部署。

## M6-06 — 独立生产执行

- 依赖任务：M6-05。
- 前提：用户明确授权具体生产发布；费用、破坏性恢复/清除仍按具体操作边界处理。
- 范围：按已验收runbook执行、生产观察与必要回退。
- 验收：记录实际发布commit、配置和迁移版本；验证Auth、Principal、兑换、文件、任务和告警；按已确认窗口观察与阈值处置。报告生产实际结果，不以Staging证据代替。

## 分阶段冻结与Git交接

本次已给出M5/M6任务链，不等M4结束再从零规划。M4-01后冻结恢复协议，T18-L后解锁文件实现，M4-10后复核M5合同/页面差距，M4-11与X04/X06明确后将运维步骤补成实测runbook。每阶段如出现公共合同变化先登记并同步消费者，不能静默降级。

当前应用提交尚未并入main。本规划分支从已推送实现基线派生，包含其历史；文档PR不能被误当作仅含规划的新分支。各实施任务小提交、指定文件暂存、脱敏差异检查、push并核对SHA；模块集成另做PR审查，只有明确合并授权才merge。
