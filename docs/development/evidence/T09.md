# T09 安全辅助表、角色与内部 helper 证据

更新日期：2026-09-07。任务分支：`task/T09-security-helpers`。

## 结论

T09 的独立 M1 基础设施交付完成。迁移 `20260907103848_security_helpers.sql` 由固定 Supabase CLI `2.111.0` 生成；Local reset、T08 回归、T09 结构和行为探针均通过。T04 依赖的真实近期 proof 验收仍单独 BLOCKED，没有用 `is_recent` 或客户端布尔值替代。

## 交付

- `private.system_admin`、`identity_lifecycle`、`platform_api_keys`、`idempotency_keys`、`admin_idempotency`、`admin_step_up`、`deletion_requests`、`deletion_jobs`、`job_leases`、`rate_limit_windows`，以及 `public.audit_logs`。
- `account_executor`、`admin_executor`、`job_executor`、`domain_owner`、`recovery_executor` 非登录、非 `BYPASSRLS` 角色组；角色密码不进入迁移。
- Platform Key HMAC 长度/小写 hex、版本/撤销/过期/生成操作约束；幂等 request hash 固定 32 字节、7 日窗口、pending/completed 合同和 Admin scope 防 NULL 唯一性漏洞。
- Admin step-up 绑定 `user_id + session_id + factor_id`，过期时间必须晚于验证时间且不超过 5 分钟；未宣称 proof 已经过真实 logout/session replacement 验证。
- 安全定义的 session/identity helper、审计 append-only、幂等 claim/finalize、job lease fencing、固定窗口限流 helper；所有 helper 固定 `search_path`，HTTP executor 不能直接执行内部 claim/finalize/audit helper。
- `packages/shared/src/database.types.ts` 已根据 Local `public` schema 更新，包含 `audit_logs`。

## 实际验证

| 验证 | 结果 | 证据 |
|---|---|---|
| V-DB-02/04 基础权限与结构 | PASS | T08+T09 pgTAP `supabase test db --local`，2 files / 54 tests；角色、RLS、无直接表 DML、审计无 update/delete policy、step-up 上限均断言 |
| V-TXN-01 基础幂等 | PASS | `tests/spikes/sql/security-helper.mjs`：同 hash pending/replay、异 hash conflict、finalize 后 completed 重放 |
| V-TXN-02 基础审计 | PASS | 同一 helper append 返回 id；运行角色权限快照确认不能直接更新审计表 |
| V-JOB-01 基础 lease | PASS | 首次 claim、活动 lease 排斥、匹配 owner/fence release、fence 递增 reclaim |
| V-DB-04 固定窗口限流 | PASS | 2 次允许，第 3 次在同窗口拒绝；只保存 32 字节 hash 与计数 |
| V-AUTH-03 identity gate 子集 | PASS | 通过 Local Auth HTTP signup 创建临时身份，写入 deleting 门闩后 `check_user_session` 返回 `identity_deleting` |
| V-AUTH-03 近期 proof 完整协议 | BLOCKED | T04 已证明 logout 后旧 access JWT 仍可用至过期；真实 proof 失效、刷新不续期、session replacement 留后续，未使用占位字段伪报通过 |

命令使用本机 Local Supabase、临时随机测试值和进程环境中的本地测试配置；输出未包含 token、密码、用户资料或数据库凭据。未执行 Staging/Production、真实 OAuth/SMTP、真实 TLS pooler 和 T10 全量 G1 负向矩阵。

## 交接

T10 可基于当前迁移和 helper 继续做三平台/三用户真实角色负向、append-only、fence 过期和双连接故障注入验收。T12/T14 涉及的近期 MFA proof 仍必须等待 T04 证据协议，不能直接把 `admin_step_up` 行当成已验证证明。
