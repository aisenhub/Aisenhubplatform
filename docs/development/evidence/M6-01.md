# M6-01 环境与备份运维协议

日期：2026-09-08  
任务：M6-01  
模块：M6 生产运行与验收  
分支：`task/M6-01-operations-protocol`  
依赖：M4-01合同冻结  
状态：DONE（设计交付；真实运维未运行）
提交：`94d0505`（已推送并核对 `origin/task/M6-01-operations-protocol`）

## 交付

新增 `docs/development/m6-operations-protocol.md`，把Local/Staging/Production隔离、角色/Secret/Origin/调度/版本漂移、M4备份删除屏障、联合manifest、外部墓碑、任务告警、恢复退出标准和X02/X04/X05/X06承接整理为可执行协议。

## 验证边界

- `pnpm run docs:check`：应验证文档导航/链接/既有任务图；本协议只增加文档引用。
- `pnpm run contracts:check`：应验证现有17个Account/32个Admin OpenAPI结构；本任务不改接口。
- 未执行备份、恢复、真实告警、容量压测、Staging或生产操作；V-OPS-01/02/03及G6保持NOT_RUN。
- 未购买服务、注入生产凭据、写入云端资源或创建数据库migration。

## 交接

M6-02负责真实联合备份/墓碑持久化，M6-03负责隔离恢复与轮换，M6-04负责容量和告警；缺外部输入时只能隔离模拟，不能宣称真实运维能力。
