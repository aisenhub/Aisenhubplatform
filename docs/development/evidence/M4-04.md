# M4-04：双层有界上传与 Storage 适配证据

日期：2026-09-09  
任务：M4-04  
分支：`task/M4-04-upload-storage`  
依赖：M4-03（PASS，Local）

## 当前结论

本任务为 **PASS（Local）**。有界读取、实例/账户并发闸门、Account API 的 upload-intent/content 两短事务边界、Consumer BFF 二进制缓冲转发、固定私有 bucket 和 server-only immutable Storage adapter 已实现；实际 Local Storage 对象写入、大小核验、finalize、同/异内容重试和超限零对象均已通过。

## 已实际验证

| 验证 | 结果 | 证据 |
|---|---|---|
| 有界读取与并发闸门 | PASS（5/5） | `deno test --allow-env supabase/functions/_shared/storage.test.ts supabase/functions/_shared/upload.test.ts` |
| T06受控接收回归 | PASS | `pnpm run test:upload`；超限/声明不符/压缩/断流在 Storage 前拒绝，provider timeout 保留 UNKNOWN_AND_BUDGET_HELD |
| Consumer BFF | PASS（7 tests） | `pnpm --filter template-preview test:unit`；binary PUT 经 server SDK，CSRF、压缩体先拒绝 |
| Account API既有回归 | PASS（13/13） | `deno test --allow-env supabase/functions/account-api/index.test.ts` |
| Workspace静态检查 | PASS | `pnpm run typecheck`、`pnpm run lint`、`pnpm exec oxfmt --check`、Deno check |
| 实际Local Storage HTTP流程 | PASS | `pnpm run test:api:m4-04-upload`；实际对象PUT/getInfo、DB finalize、同内容重试、异内容409及超限零对象通过 |

## 变更范围

- `supabase/migrations/20260908152641_m4_04_upload_storage.sql`：私有 bucket、状态读取、unknown 保留和可信 finalize 过程；不在SQL事务内调用Storage。
- `supabase/functions/_shared/upload.ts`、`storage.ts`及测试：最多limit字节读取、identity编码、15秒接收预算、16/实例与2/账户闸门、固定 bucket/path、`upsert=false`和四个受控adapter操作。
- `supabase/functions/account-api/index.ts`：upload-intent及content PUT；claim/prepare提交后才调用Storage，超时/核验失败记录unknown，不重复PUT。
- `packages/account-server/src/index.ts`、`apps/template-preview/app/api/v1/[...path]/route.ts`及测试：server-only SDK、BFF有界二进制转发和CSRF/Origin约束。
- `supabase/tests/m4_04_upload_storage.sql`、`tests/spikes/api/m4-04-upload.mjs`：结构/权限与真实Local Storage流程验证入口。

## 交接与限制

Storage secret 未进入仓库或输出。M4-05 应继续复用本任务的 Storage adapter 和 `unknown` 保留边界；不把本地 Storage 通过扩大为托管或生产验收。
