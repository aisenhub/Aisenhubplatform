# M4-08：最小文件管理与消费 UI 证据

日期：2026-09-09  
任务：M4-08  
分支：`task/M4-08-file-ui`  
依赖：M4-05、M4-06、M4-07（Local PASS）

## 当前结论

本任务为 **PASS（Local）**。Consumer 已提供意图→有界上传→状态查询→下载→Replace/删除路径；Admin 已提供文件策略读取/更新、状态、近期 MFA 下载和受控删除页面。预算由领域函数读取，页面不直连 Storage、不计算配额、不自动重发上传。

## 已实际验证

| 验证 | 结果 | 证据 |
|---|---|---|
| M4-08 预算读取、策略读取、角色授权、SECURITY DEFINER/search_path 与负向边界 | PASS（15/15） | `pnpm test:db`；17 个 pgTAP 文件、331 个断言全部通过 |
| Consumer/Admin 页面本地浏览器空/错误状态、上传控件、刷新、导航与可访问名称 | PASS（Local） | `http://localhost:3100/files`、`http://localhost:3101/admin/files`；CUA AX tree 与截图核对 |
| 应用类型、Lint、合同和构建 | PASS | `pnpm typecheck`、`pnpm lint`、`pnpm run contracts:check`、`pnpm format:check`、两个 Next build |
| Impeccable UI 检测 | PASS（无 error；仅报告已有字体与 warning accent） | `node C:\Users\S1786\.codex\skills\impeccable\scripts\detect.mjs --json ...` |

## 变更范围

- `supabase/migrations/20260908175926_m4_08_file_ui_support.sql`：Account 预算读取和 Admin 策略读取领域入口。
- `supabase/functions/account-api/index.ts`、两端 BFF、OpenAPI：预算、策略、文件状态和安全下载路径。
- `apps/template-preview/app/files/page.tsx`、`apps/admin/app/admin/files/page.tsx`：Consumer 与 Admin 文件页面。
- `supabase/tests/m4_08_file_ui_support.sql`：领域权限与预算负向测试。

## 交接与限制

以上为 Local 证据，不升级为托管、真实 Storage 或生产验收。Admin 删除返回 202 仅表示领域任务已受控提交，不表示对象已从 Storage 删除；实际外部清除和故障恢复属于 M4-09。
