# M4-07：元数据查询与下载代理证据

日期：2026-09-09  
任务：M4-07  
分支：`task/M4-07-file-query-download`  
依赖：M4-04、M4-05、M4-06（均为 Local PASS）

## 当前结论

本任务为 **PASS（Local）**。Account 文件列表/详情通过受控 SQL 返回状态、预算占用和安全 metadata；Account 与 Admin 下载先在短事务内完成归属、active/confirmed 和近期 MFA 校验，再由后端 Storage adapter 代理私有对象。分页默认 20、上限 100，游标按 `created_at,id` 稳定排序；下载响应固定二进制安全头，缺失对象记录失败审计并返回不可用，不返回 Storage URL。

## 已实际验证

| 验证 | 结果 | 证据 |
|---|---|---|
| 查询/下载领域函数、SECURITY DEFINER、角色权限、无直接表/Storage metadata访问 | PASS（29/29） | `pnpm run test:db`；16 个 pgTAP 文件、316 个断言全部通过 |
| Account API 列表、详情、下载、跨平台拒绝、Suspend拒绝、缺失对象失败审计 | PASS | `pnpm run test:api:m4-07-file-query-download`；`fileListAndState`、`secureDownload`、`crossTenantAndSuspendedDenied`、`missingObjectAlertAudit` 均 PASS |
| Account API 二进制流安全头与流完成审计 | PASS（17/17） | Deno `account-api/index.test.ts`；包含真实 ReadableStream 代理测试 |
| SDK、BFF、OpenAPI、DTO与文档合同 | PASS | `pnpm run test:unit`、`pnpm run typecheck`、`pnpm run contracts:check`、`pnpm run docs:check`、`pnpm run format:check` |

## 变更范围

- `supabase/migrations/20260908173042_m4_07_file_query_download.sql`：Account/Admin 列表、详情、下载授权和流完成/失败审计入口；查询只返回受控 metadata，下载不返回对象路径。
- `supabase/functions/account-api/index.ts`：稳定游标、Account/Admin HTTP 包装、近期 MFA 下载门槛、Storage 代理、安全文件名和流审计。
- `packages/domain/src/contracts/api.ts`、`packages/account-server/src/index.ts`：文件状态/预算 DTO、列表/详情/二进制下载 SDK。
- `apps/template-preview/app/api/v1/[...path]/route.ts`：Consumer BFF 的列表、详情、下载转发，平台 Key 保持服务端。
- `docs/contracts/account.openapi.json`、`docs/contracts/admin.openapi.json`、测试与探针：同步 local-only 状态、分页和二进制合同。

## 交接与限制

以上为 Local 证据，不升级为托管或生产验收。Admin 文件页面仍由 M4-08 交付；Admin 删除、deletion-jobs 和策略页面仍保持 contract-only。缺失对象告警当前以受控 `file.download_failed` 审计事件承载，外部告警投递属于后续运维集成范围。
