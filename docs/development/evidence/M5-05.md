# M5-05：独立 Consumer 安装验收证据

日期：2026-09-09  
任务：M5-05  
分支：`task/M4-08-file-ui`  
状态：IN_PROGRESS（Local install；非G5-L）

## 已实际验证

| 验证 | 结果 | 证据 |
|---|---|---|
| 空隔离目录从本次四个 SDK tarball 安装 | PASS | `pnpm run test:consumer:m5-05`：`independentConsumerInstall: PASS` |
| Consumer 未使用 workspace link | PASS | `workspaceLinks: ABSENT`；内部 `@kit/*` 通过本次 tarball override 解析 |
| 独立 tsconfig/typecheck | PASS | `typecheck: PASS`；fixture 不继承 monorepo `../../tsconfig.json` |
| 独立 Next production build | PASS | `build: PASS` |
| Hosted 双 Origin/Platform Auth/刷新/激活/Profile/兑换/文件/停用 E2E | NOT_RUN | `hostedDualPlatformE2E: NOT_RUN (X05/hosted backend unavailable)` |

## 故障修复

- 首次安装暴露 SDK tarball 的内部 `@kit/*@0.1.0` 依赖需要固定到同批 tarball，fixture 改为 pnpm 本地 tarball overrides，不回退到 workspace。
- 离开 monorepo 后 template-preview 的 `../../tsconfig.json` 不存在，fixture 生成独立 TypeScript 配置并显式安装测试类型依赖。
- 离线 store 缺少公开 Next 依赖时改用固定版本的 `--prefer-offline` 安装；未安装系统软件。

## 限制与下一步

本证据只覆盖 Local packaging/install/build，不满足 M5-05 规定的双平台真实业务链路，也不关闭 M5-05/G5-L。需要 X05、隔离测试后端和受控托管 Origin 后再执行 hosted E2E 与浏览器 bundle 回归。
