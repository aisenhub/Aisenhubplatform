# T03 双运行时公共边界、脚本与 CI

日期：2026-09-07  
任务：T03  
分支：`task/T03-runtime-ci`  
依赖：T02 commit `1d62434ef05a6f4ee217b8098c870f8507e74412`

## 结果

T03 验收：PASS。M0 尚未完成，G0-L/G0-S 仍按真实条件记录为 `NOT_RUN`。

## 变更

- 新增 `packages/domain` 纯 TypeScript 公共边界，包含 `DomainResult`、可序列化 `DomainError`、请求上下文、Principal DTO 和 UUID/非空校验；不依赖 React、Next、Node 专属 API 或生产权益算法。
- `supabase/functions/_shared/runtime-probe.ts` 直接从 `packages/domain/src` 导入共享实现；Node 和 Deno 探针均导入同一文件，没有复制第二份领域实现。
- 新增真实 Vitest 合同用例、运行时探针、固定 CLI 包装和文档检查器。
- 根命令补齐 `test:unit`、本地 Supabase `db:start/db:stop/db:reset`、实际 `test:db`、运行时探针和 `docs:check`。API/E2E 尚无夹具时返回明确 `NOT_RUN` 非零状态，不伪造成功。
- 本地 Supabase 包装拒绝非 localhost URL 和 `SUPABASE_PROJECT_REF`，不提供生产操作入口。
- CI 改为只读质量检查：Node `24.19.0`、pnpm `11.18.0`、Deno `2.9.6`；执行 format/lint/typecheck/build/unit/runtime/docs 检查，不执行部署、发码或生产迁移，也不向 PR 注入 Secret。
- README 增加可复现命令、工作目录和当前未启用测试阶段说明。

## 固定工具

| 项目 | 结果 |
|---|---|
| Node | `24.19.0` |
| pnpm | Corepack `11.18.0`，项目 `.pnpm-store` |
| Supabase CLI | 项目依赖 `2.111.0`，禁用遥测后项目二进制输出 `2.111.0` |
| Deno | `D:\APP\Codex\Deno\bin\deno.exe`，`2.9.6` |
| Docker | Docker Desktop `29.7.2`，本任务未启动 Local Supabase |

## 验证

| 用例/检查 | 结果 | 实际证据 |
|---|---|---|
| V-BASE-01 固定依赖安装 | PASS | `pnpm install --frozen-lockfile --offline`，9 workspace，退出码 0 |
| V-BASE-02 Admin/Consumer 构建 | PASS | 根 build，Admin 与 Consumer 共 2 个 build task 成功 |
| format:check | PASS | Oxfmt 198 个文件通过 |
| lint | PASS | Oxlint 退出码 0 |
| typecheck | PASS | Turbo 当前 6 个实际 typecheck task 成功 |
| test:unit | PASS | Domain 2 tests、UI 35 tests，共 37 tests |
| Node/Edge 双运行时 import | PASS | Node 探针与 Deno `2.9.6` 探针均输出 PASS |
| docs:check | PASS | 相对链接、18 个任务依赖无环、验证 ID 一致 |
| SP-SOURCE | PASS | 继承 T01 固定上游/脚本审查证据 |
| SP-SQL / SP-AUTH / SP-UPLOAD / SP-TXN | NOT_RUN | 分别由 T05/T04/T06/T07 实际探针交付 |
| test:db | NOT_RUN | 固定 CLI 命令已建立，尚无 SQL/pgTAP 测试集且未启动数据库 |
| test:api / test:e2e | NOT_RUN | 夹具尚未建立，命令按设计明确返回 `NOT_RUN` |
| G0-L / G0-S | NOT_RUN | Local 数据库和托管资源尚未验证 |

## 限制与交接

- Deno 安装遵循 `AGENTS.md` 的 D 盘规则；CI 使用 `denoland/setup-deno@v2` 的固定版本。
- 本地 Supabase CLI 验证使用 `SUPABASE_TELEMETRY_DISABLED=1`，避免向用户目录写遥测文件；未保存任何凭据。
- T04、T05、T06 依赖已满足，均可从本提交分别派发；T07 仍等待 T05。
