# T14 激活、状态控制、关闭与删除请求证据

更新日期：2026-09-07。任务分支：`task/T14-account-lifecycle`。

## 结论

T14 Local 验收通过。激活按平台与身份状态检查并在同一事务创建 Profile/Preferences；重复激活返回既有账户；Admin suspend/restore/close 使用账户锁和统一审计；用户Close与Global Delete都要求服务端绑定user/session的五分钟proof，删除请求只进入`pending_admin`，不启动清除。

## 交付

- `supabase/migrations/20260907124124_t14_account_lifecycle.sql`：近期proof存储、account_activate/account_close/identity_delete_request/admin_account_transition。
- `supabase/tests/t14_account_lifecycle.sql`：函数、RLS、角色权限、proof时间边界和search_path回归。
- `tests/spikes/sql/t14-account-lifecycle.mjs`：真实Local Auth用户、激活幂等、Admin状态、proof门禁和pending_admin行为探针。

## 实际验证

| 验证 | 结果 | 证据 |
|---|---|---|
| T08-T14数据库回归 | PASS | `supabase test db --local`：5 files / 89 tests |
| 激活与初始化资料 | PASS | Local Auth用户激活后生成唯一账户、Profile和Preferences，重复激活返回同一账户 |
| Admin状态迁移 | PASS | 临时system_admin经admin_executor执行suspend→restore，状态和审计路径通过 |
| Close proof门禁 | PASS | 缺proof返回42501；同user/session且不超过5分钟的proof允许close |
| Global Delete前置门闩 | PASS | delete-request重复请求复用同一pending_admin记录，没有Auth删除或deleting状态副作用 |
| 类型/格式/静态检查 | PASS | account-server与auth单测；oxfmt check、oxlint |

## 未完成与交接

- 真实Provider reauthentication/proof发行和已签发JWT logout失效仍受T04/SP-AUTH阻塞；本任务没有把受控proof存储冒充为完整Auth验收。
- 生产HTTP Account路由、Admin页面和Profile/Preferences/Plans读写仍由T15及后续纵向任务接入。
- 下一项满足依赖的任务是T15 Profile、Preferences与公开Plan。
