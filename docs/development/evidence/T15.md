# T15 Profile、Preferences与公开Plan证据

更新日期：2026-09-08。任务分支：`task/T15-profile-preferences-plans`。

## 结论

T15 Local 验收通过。Profile 使用字段白名单和乐观版本条件更新；Preferences 使用有界 JSON Merge Patch；两者均由统一 principal 读取并在版本冲突时拒绝。公开 Plan 读取只接受有效的 active platform/key，平台停用后拒绝，不创建账户。

## 交付

- `supabase/migrations/20260907125544_t15_profile_preferences_plans.sql`：Profile/Preferences 读写、版本条件、公开 Plan 内部函数及最小权限包装。
- `supabase/tests/t15_profile_preferences_plans.sql`：函数存在性、执行权限、RLS、security definer/search_path和触发器回归。
- `tests/spikes/sql/t15-profile-preferences-plans.mjs`：真实 Local Auth 用户、Profile/Preferences 更新、过期版本、字段边界、公开 Plan和停用平台行为探针。

## 实际验证

| 验证 | 结果 | 证据 |
|---|---|---|
| T08-T15数据库回归 | PASS | `supabase test db --local`：6 files / 108 tests |
| Profile读写与版本冲突 | PASS | Local Auth用户激活后读取version 1，白名单PATCH返回version 2，过期版本返回40001 |
| ownership字段边界 | PASS | Profile PATCH携带ownership字段返回22023 |
| Preferences Merge Patch | PASS | 非空值合并、JSON null删除键并递增row_version |
| 公开Plan边界 | PASS | active platform/key返回白名单套餐；平台disabled后返回28000 |
| 类型/格式/静态检查 | PASS | T15文件格式、oxlint、文档检查、根typecheck和git diff检查 |

## 未完成与交接

- HTTP路由、ETag/If-Match/428/412、no-store header和前端表单仍由T16纵向集成接入；本任务没有创建假成功handler。
- Plan管理、权益计算、订阅事件和兑换码不在T15范围，留给M3。
- 下一项满足本地依赖的任务是T16最小Consumer/BFF纵向集成；T17仍受X01/X02/X03托管输入约束。
