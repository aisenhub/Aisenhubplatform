# M4-01 文件与恢复合同冻结

日期：2026-09-08  
任务：M4-01  
模块：M4 配置文件与持久任务  
分支：`task/M4-01-file-contract`  
依赖：无  
状态：DONE（合同/规格交付；M4运行实现仍未开始）

## 交付

在 `docs/config-files.md` 增加M4-01冻结附录，明确：

- `file_intent_create`、`file_receive_claim`、`file_prepare_store`、`file_write_attempt_settle`、`file_delete_request`、`file_reconcile_step`及备份屏障入口的调用角色、context、锁顺序、状态和拒绝语义。
- `status` 与 `write_outcome` 分离；`unknown`持续占用预算，不因404、租约过期或超时猜测释放。
- 文件名/metadata边界、分页游标、202处理中语义、六个Account文件操作及Admin文件/策略/删除任务映射；明确禁止浏览器complete、signed upload和Storage直连。
- M4/M6共享持久备份删除屏障、2小时失败优先规则、manifest字段和非Secret最小删除墓碑；M6负责主库外持久化，M4不虚构联合备份已完成。

## 静态验证

- `pnpm run docs:check`：PASS，文档链接、首批任务依赖图和验收ID一致。
- `pnpm run contracts:check`：PASS，Account 17个、Admin 32个操作及引用/二进制/no-store检查通过。
- `git diff --check`：PASS。
- Oxfmt：NOT_RUN；仓库配置排除Markdown，不能将“无匹配文件”写成格式化PASS。

## 验收边界

| 范围 | 合同覆盖 | 运行验证 |
|---|---|---|
| V-FILE-01～07 | PASS（静态矩阵与失败语义已登记） | NOT_RUN，待M4-02～M4-10 |
| V-JOB-01 | PASS（lease/fence/retry/unknown语义已登记） | NOT_RUN，待M4-05 |
| V-DELETE-01 | PASS（屏障/墓碑/门闩交接已登记） | NOT_RUN，待M4-09/M6 |
| G4-L/G4-S | NOT_RUN | M4-10/M4-11判定，不由本合同任务宣称 |

## Git交接

本任务只修改合同/证据/状态文档，不新增migration、表、函数或云端资源。提交和远端SHA在本任务提交后补入；下一项实现任务为T12-R2（其余M4实现仍等待T18-L）。
