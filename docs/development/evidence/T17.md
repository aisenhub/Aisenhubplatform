# T17 托管环境探针与真实 Auth 验收

日期：2026-09-08  
项目：`workendstaging` / `egsokuicabbxspkdccqe`  
状态：PARTIAL（真实 Auth/Account API 基础链路通过；X02/X03、托管 pooler/TLS 和 Storage 仍待验收）

## 已完成

| 检查项 | 结果 | 证据 |
|---|---|---|
| Staging 项目身份与健康 | PASS | Dashboard 项目状态 Healthy；Auth health HTTP 200；GoTrue `v2.196.0` |
| 旧 Staging 基线清理 | PASS | 旧 `platform` schema、旧 public 函数、6 个旧 Edge Functions、旧自定义 Edge Secrets、旧 Auth 用户已清理 |
| 仓库迁移应用 | PASS | 13 个仓库迁移已应用；迁移名与仓库迁移集对应；目标 `public`/`private` 表存在且当前为空 |
| RLS 基线 | PASS | 当前 25 张目标业务表均启用 RLS；未自动放宽权限 |
| `account-api` 托管部署 | PASS | Edge Function v13 ACTIVE，`verify_jwt=true`；无平台 Key 的入口请求返回 HTTP 401 |
| 本机 Staging 配置 | PASS | 根目录 `.env` 已配置 URL、Publishable Key 和 Staging HMAC Secret；文件被 Git 忽略 |
| 真实 Auth/Account API 基础链路 | PASS | 登录、plans、未激活 principal、activate、激活后 principal、profile、preferences、subscription 均按合同返回；logout 后旧 JWT 返回 HTTP 401 |
| 本轮探针 fixture 清理 | PASS | 临时 Auth 用户、平台、计划、Key、账户和审计记录均已清理；用户列表保留用户创建的 Staging 账户 |

## 仍未完成

| 检查项 | 结果 | 原因 |
|---|---|---|
| 真实 Auth signup/login/session | PASS | 使用 Dashboard 创建的临时用户完成密码登录和 JWT session；探针结束后已删除临时用户 |
| 真实用户 Account API 正向链路 | PASS | 完成 plans、principal、activate、profile、preferences、subscription 基础路径 |
| logout 后旧 JWT 即时失效 | PASS（Staging） | Auth logout 返回 HTTP 204；同一旧 JWT 访问 principal 返回 HTTP 401 |
| 真实浏览器 SSR | NOT_RUN | 尚无实际 Admin/BFF 托管站点、域名和可信代理链（X02） |
| Google OAuth / SMTP | NOT_RUN | X03 配置尚未提供 |
| Storage 取消/迟到写入 | NOT_RUN | 仍属于真实 Edge/Storage host 验收 |

## Advisor 结果

- Security：`public.platform_auth_origins` 为 RLS enabled/no policy（INFO，符合默认拒绝设计）；Auth 泄露密码保护未启用（WARN）。旧 `platform.idempotency_records` Critical 告警已随旧 schema 清理而不存在。
- Performance：仅有空 Staging 下的未覆盖外键和未使用索引 INFO 项；本轮未把这些静态提示擅自改成业务迁移。

## 结论

T17 已完成 X01 Staging 基线、仓库迁移、Edge 部署和真实 Auth/Account API 基础探针，状态为 PARTIAL。X02 仍需实际 Admin/BFF 托管站点、域名和可信代理链；X03 仍需 Google OAuth/SMTP；托管 pooler/TLS、浏览器 SSR 与 Storage 取消语义仍待验收。报告不把 `NOT_RUN` 记为 PASS。
