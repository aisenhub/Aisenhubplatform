# T16-R1：M2 端到端资源补齐证据

日期：2026-09-08  
任务：T16-R1  
分支：`task/T16-R1-account-vertical`  
依赖：T12-R2（PASS，Local）

## 当前结论

本任务为 **PASS（Local）**。M2 的平台、Origin、Platform Key、账户管理 HTTP→SQL 链路，普通账户 Close/Global Delete request 消费者，SDK/BFF 和最小管理/账户页面已实现并通过本地回归。T16-R2 继续负责两个不同入口的真实浏览器流程、可复现 CI 运行器及其负向场景；M4/M5/M6、托管 Supabase/Auth/SMTP/Storage 与生产部署不在本任务结论内。

所有管理写入均经中央 Account API 的 Admin/AAL2 门禁和受控 SQL wrapper；前端没有直接写表。新建 Platform Key 的原始值只在创建响应中一次性返回，数据库列表与审计路径不返回 HMAC 或明文 Key。

## 实际验证

| 验证 | 结果 | 证据边界 |
|---|---|---|
| Local pgTAP 全量 | PASS（10 files / 184 tests） | `pnpm run test:db`；包含 T16 M2 wrapper/RLS、跨目标和权限回归 |
| Account API Deno 单测 | PASS（13/13） | `pnpm exec deno test --allow-env --allow-net supabase/functions/account-api/index.test.ts` |
| T12 普通双 session proof 实际 API | PASS | `T12_PLATFORM_KEY_HMAC_SECRET=local-fixture-only... pnpm run test:api:t12-ordinary-proof`；真实 Local Auth/Mailpit/中央 API，输出七项 PASS |
| T16 M2 管理实际 HTTP→SQL | PASS | `pnpm run test:api:t16-m2-management`；AAL2 管理员创建 Platform/Origin/Key，确认 Key secret 不出现在列表，激活用户并完成账户 list/suspend/restore/close |
| Account SDK 单测 | PASS（10/10） | `pnpm --filter @kit/account-server test:unit` |
| Template Preview BFF 单测 | PASS（5/5） | `pnpm --filter template-preview test:unit` |
| 类型检查 | PASS | `pnpm run typecheck` |
| Admin 类型检查 | PASS | `pnpm --filter admin typecheck` |
| Lint/格式 | PASS | `pnpm run lint`、`pnpm run format:check` |
| OpenAPI 合同 | PASS | `pnpm run contracts:check`；Account 18、Admin 32 operations |
| 文档一致性 | PASS | `pnpm run docs:check` |
| Admin production build | PASS | 包含 `/admin/platforms` 与平台管理 BFF |
| Template Preview production build | PASS | 包含 `/account`、reauth BFF 与敏感操作 BFF |

## 变更范围

- `supabase/migrations/20260908134837_t16_m2_management.sql`：新增受控 domain-owner 管理 wrapper；`20260908140402_t16_m2_management_rls_fix.sql`、`20260908140621_t16_account_list_ambiguity_fix.sql` 为 Local 实测发现问题追加的修复迁移。
- `supabase/functions/account-api/index.ts`：M2 Admin 平台/Origin/Key/账户路由及普通账户 Close/Delete request 中央入口。
- `packages/account-server`、`apps/admin/app/api/v1/[...path]`、`apps/template-preview/app/api/v1/[...path]`：server-only proof/header、JSON Merge Patch、ETag/敏感操作 BFF 适配。
- `apps/admin/app/admin/platforms`、`apps/template-preview/app/account`：最小平台管理和账户设置页面。
- `supabase/tests/t16_m2_management.sql`、Account API/SDK/BFF 单测及 `tests/spikes/api/t16-m2-management.mjs`：结构、权限与真实 Local HTTP 回归。
- `docs/contracts/admin.openapi.json`、`docs/contracts/account.openapi.json`、`docs/api-sdk.md`：实现状态和普通近期认证协议同步。

## 交接与限制

T16-R2 可从本分支继续：须为两个不同 Origin/Platform 启动可清理的 Local fixture，接入明确的 `test:e2e`/CI 命令，并验证刷新、Cookie 隔离、CSRF/returnTo、AAL1、过期 proof、停用后拒绝和中央不可用拒绝。测试缺少服务依赖时必须显式失败或 NOT_RUN，不能静默跳过。Hosted/SMTP/Storage/生产证据仍保持未宣称状态。
