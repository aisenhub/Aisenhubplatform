# T12 Auth SDK、SSR adapter与Admin鉴权证据

> DP2校准（2026-09-08）：本文是原任务交付快照，以下NOT_RUN/阻塞描述属于当时。后续T04/T17已补密码Auth、Admin proof、logout旧JWT拒绝及Staging基础路径，不能再把这些列为全部缺失；T12仍PARTIAL，真实SSR与普通reauth由[收尾T12-R1/R2](../tasks/closeout-01.md)完成。原测试结果不改写。

更新日期：2026-09-08。任务分支：`task/T12-auth-adapters`。

## 结论

T12已交付可独立验证的认证边界：框架无关Auth intent、服务端session/revocation校验、session-bound近期证明、SSR每请求client、CSRF同源校验、相对returnTo和Admin membership/AAL2/step-up门禁。真实Supabase Auth HTTP流程和T04已签发JWT退出失效边界仍未伪造为通过，因此任务整体标记为PARTIAL/BLOCKED。

## 交付

- `packages/account-auth`：密码/Provider/link intent、session verifier和五分钟近期证明校验。
- `packages/account-auth-nextjs`：每请求client工厂、安全Cookie策略、CSRF同源校验、returnTo约束和no-store头。
- `packages/account-server`：普通用户与Admin实时session/membership/AAL2/敏感proof门禁。
- workspace lockfile：登记新增内部包及其依赖关系。

## 实际验证

| 验证 | 结果 | 证据 |
|---|---|---|
| session、revocation、expected user | PASS | account-auth单测覆盖有效、缺失、错误用户和过期会话 |
| session-bound recent proof | PASS | proof必须同user/session，过期或超过5分钟拒绝 |
| SSR隔离与CSRF | PASS | 每请求factory、Cookie不共享、Origin不匹配拒绝、响应no-store |
| Admin门禁 | PASS | membership、AAL2和敏感入口近期proof单测通过 |
| 类型/单测/格式/静态检查 | PASS | turbo typecheck；4 files / 10 tests；oxfmt check、oxlint |
| 真实Local Auth流程 | NOT_RUN | 本任务只使用注入式provider接口，未把mock当真实Supabase验收 |

## 未完成与交接

- T04/SP-AUTH仍阻塞：已签发access JWT在logout后的失效边界没有可靠服务器证据；生产Close/Global Delete不能仅依赖本包的证明类型。
- @supabase/ssr与supabase-js实际adapter、Cookie/PKCE/Callback端到端、真实Admin membership/AAL2取数仍需在依赖可用的Auth任务中完成。
- T13可基于当前session/context合同继续实现Platform Key、Principal和Admin平台基础；敏感Close/Delete仍须保留T04门禁。
