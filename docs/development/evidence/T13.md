# T13 Platform Key、Principal与Admin平台基础证据

> DP2校准（2026-09-08）：本文Local通过对应所列SQL/SDK范围。后续中央基础API及T04/T17认证已有进展；历史缺项不等于当前全部缺失。完整M2 HTTP/最小Admin管理由[收尾T16-R1](../tasks/closeout-01.md)承接，托管Origin由T17-R2验收。

更新日期：2026-09-08。任务分支：`task/T13-platform-key-principal`。

## 结论

T13 Local 验收通过。Platform Key 只以版本化HMAC定位，Principal由服务端session、Key所属平台、平台状态和用户账户关系实时解析；Admin平台更新与Key撤销通过session-bound Admin context和审计包装完成。禁用平台可诊断但不能授权，跨平台Key被拒绝。

## 交付

- `supabase/migrations/20260907121722_t13_platform_key_principal.sql`：Key校验、Principal解析、Admin平台更新和Key撤销函数，固定search_path、security definer和最小角色EXECUTE。
- `packages/account-server`：CSPRNG Platform Key材料/HMAC、Origin和Callback校验，以及T12授权guard扩展。
- `tests/spikes/sql/t13-platform-key.mjs`：Local Auth用户、跨平台、禁用、Admin更新和撤销后的真实行为探针。
- `supabase/tests/t13_platform_key_principal.sql`：函数、RLS、角色权限和search_path pgTAP回归。

## 实际验证

| 验证 | 结果 | 证据 |
|---|---|---|
| T13 pgTAP权限/函数回归 | PASS | `supabase test db --local`：4 files / 73 tests |
| Platform Key与跨平台隔离 | PASS | `t13-platform-key.mjs`：platformKey/crossPlatform/revoke均PASS |
| Principal实时状态 | PASS | active账户返回allowed；平台disabled返回platform_disabled；无跨目标授权 |
| Admin平台动作 | PASS | 临时Local Auth用户授予system_admin并用admin_executor执行更新/撤销，审计路径有效 |
| SDK key/origin边界 | PASS | account-server单测包含CSPRNG/HMAC长度、origin规范化和callback host校验 |
| 类型/单测/格式/静态检查 | PASS | turbo typecheck；4 files / 11 tests；oxfmt check、oxlint |

## 未完成与交接

- Key明文只存在生成响应的进程内，未写入数据库或日志；实际HTTP Account路由和Admin页面仍由T14/T15及后续任务接入。
- 真实托管Origin同步依赖X01/X02/X03，当前未运行；T04近期认证阻塞仍适用于Close/Global Delete，不因T13通过而解除。
- 下一项满足依赖的任务是T14账户生命周期。
