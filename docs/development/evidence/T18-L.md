# T18-L：G1/G2-L 本地阶段收口证据

日期：2026-09-08  
任务：T18-L  
分支：`task/T18-L-local-closeout`  
候选 commit：`481ba99868fcdb2eb1cd6cbaab4904bcc675f5b4`  
远端核对：`origin/task/T18-L-local-closeout` 与 `origin/task/T16-R1-account-vertical` 均指向候选 commit

## 当前结论

T18-L 为 **PASS（Local）**。候选 commit 在 Local 空库 reset 后从完整迁移集升级，真实角色 pgTAP、中央 Account API、SDK/BFF、两个生产应用和真实 Chrome 双入口回归均通过。G1/G2-L 本地门槛可以交接给 M4-02；G0-S/G2-S、真实 Provider/SMTP、托管 executor/TLS/Storage 和生产部署仍未通过，不因本报告升级。

## 实际验证

| 门槛/验证 | 结果 | 实际命令或证据 |
|---|---|---|
| Local 空库迁移升级 | PASS | `pnpm run db:reset`；固定 Supabase CLI 2.111.0 从基线迁移应用至 T16 修复迁移 |
| G1 数据库/真实角色 | PASS | `pnpm run test:db`；10 files / 184 tests，含角色负向、审计/幂等、生命周期、Profile/Preferences 和 M2 管理 |
| Account API 负向/合同路径 | PASS | `deno test --allow-env --allow-net supabase/functions/account-api/index.test.ts`；13/13 |
| T12 普通 proof API | PASS | `pnpm run test:api:t12-ordinary-proof`；email event、独立 session、中央 proof、原 session 绑定、delete、close、临时 session 撤销全部 PASS |
| T16 M2 管理 API | PASS | `pnpm run test:api:t16-m2-management`；平台、Origin、Key secret 不回显、账户 list/suspend/restore/close 全部 PASS |
| G2-L 浏览器 | PASS | `pnpm run test:e2e`；双 Origin/Platform、独立 context、Profile/Preferences、CSRF/ETag、Admin AAL1/MFA/停用恢复、普通 proof、Close/Delete、bundle 扫描 8 项 PASS |
| 根单元测试 | PASS | `pnpm run test:unit`；domain 3、ui 35、auth 3、auth-nextjs 7、account-server 10、template-preview 5 |
| 类型/静态/合同/文档 | PASS | `pnpm run typecheck`、`pnpm run lint`、`pnpm run format:check`、`pnpm run contracts:check`、`pnpm run docs:check` |
| 候选构建 | PASS | `pnpm run build`；Admin 与 Template Preview production build 完成 |

## 归属与限制

- T12-R2、T16-R1、T16-R2 的 Local 证据已分别保留；本报告只确认候选 commit 上的统一复验，不把历史 `NOT_RUN` 改写为当前已运行的 hosted/production 证据。
- Admin 包没有独立 `test:unit` script；本次未将不存在的命令写成 PASS，根 `test:unit` 已按 Turbo 实际存在的 6 个测试包完成。
- T18-S 继续承接 hosted Auth/SMTP、独立 executor/TLS/CA/pooler、真实 Provider、Storage 语义和生产证据；M4-02 可按计划依赖本地 G1/G2-L 证明开始。

## Git 交接

T16-R1/T16-R2 修复与证据已推送；T18-L 候选分支已推送并用 `git ls-remote` 核对。未合并或覆盖 `main`，未执行生产部署、费用变更或破坏性恢复。
