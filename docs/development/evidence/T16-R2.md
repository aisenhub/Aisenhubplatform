# T16-R2：双入口浏览器验收与测试入口证据

日期：2026-09-08  
任务：T16-R2  
分支：`task/T16-R1-account-vertical`（承接 T16-R1 实现）  
依赖：T16-R1（PASS，Local）

## 当前结论

本任务为 **PASS（Local）**。`pnpm run test:e2e` 已接入真实 Local runner；runner 自动读取 Local Supabase 状态，在本地创建随机用户、管理员/TOTP、两个 Platform/Key，启动中央 Account API、两个不同 Origin 的 Consumer production server 和 Admin production server，使用独立 Chromium contexts 执行并清理 fixture。

## 实际验证

| 验证 | 结果 | 证据边界 |
|---|---|---|
| 双 Origin/Platform Consumer 登录与激活 | PASS | Consumer A/B 分别使用独立端口、Platform Key 和浏览器上下文；Principal platform_id 与 Key 映射一致 |
| Cookie 隔离 | PASS | 两个 context 的 HttpOnly session 值不同；未共享 proof/session 状态 |
| Profile/Preferences | PASS | 真实页面提交 Profile 与 JSON Merge Patch Preferences，经 BFF→Account API→SQL |
| CSRF/ETag | PASS | 缺 CSRF 返回 403；过期 `If-Match` 返回 412 |
| 普通近期认证与敏感动作 | PASS | Local Mailpit `token_hash`→独立事件 session→proof cookie→Global Delete request 202→Account Close 200 |
| Admin AAL1/MFA/停用恢复 | PASS | AAL1 管理 API 403/MFA_REQUIRED；真实 Admin MFA 后从平台页面 suspend，Consumer 受保护路由 403/ACCOUNT_SUSPENDED，再 restore |
| Browser bundle 凭据扫描 | PASS | Admin/Consumer `.next/static` 未发现 Platform Key、HMAC secret、DB URL、Service Role 或 Supabase secret 标识 |
| 根测试入口 | PASS | `pnpm run test:e2e` 与 `pnpm run test:e2e:t16-r2` 使用同一 runner，输出 8 项 PASS |

## 变更范围

- `tests/spikes/e2e/t16-r2-account.mjs`：可复现的 Local fixture、中央/应用进程编排、双上下文浏览器流程、负向验证、bundle 扫描和精确清理。
- `package.json`：`test:e2e` 从占位命令接入 T16-R2 runner，并保留任务专用别名。
- `docs/development/tasks/closeout-01.md`、`docs/development/status.md`：更新 T16-R2 Local 结论并把 T18-L 标为 READY。

## 限制与交接

本证据不代表托管 CI 已运行，也不覆盖 hosted Auth/SMTP/Storage、TLS、生产部署或跨真实域名 Provider 回调。中央不可用、proof 过期、returnTo 及更广泛多用户业务回归仍需在 T18-L/后续 CI 矩阵中持续保留；Local 服务未启动时 runner 应失败而不是静默跳过。T18-L 现在可以按候选 commit 重跑 G1/G2-L 收口。
