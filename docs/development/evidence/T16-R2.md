# T16-R2：双入口浏览器验收与测试入口证据

日期：2026-09-08  
任务：T16-R2  
分支：`task/T16-R1-account-vertical`（承接 T16-R1 实现）  
依赖：T16-R1（PASS，Local）

## 当前结论

本任务为 **PASS（Local）**。`pnpm run test:e2e` 已接入真实 Local runner；runner 自动读取 Local Supabase 状态，在本地创建随机用户、管理员/TOTP、两个 Platform/Key，启动中央 Account API、两个不同 Origin 的 Consumer production server 和 Admin production server，使用独立 Chromium contexts 执行并清理 fixture。

## 实际验证

| 验证 | 结果 | 证据边界 |
|---|---|---|
| 双 Origin/Platform Consumer 登录与激活 | PASS | Consumer A/B 分别使用独立端口、Platform Key 和浏览器上下文；Principal platform_id 与 Key 映射一致 |
| Cookie 隔离 | PASS | 两个 context 的 HttpOnly session 值不同；未共享 proof/session 状态 |
| Profile/Preferences | PASS | 真实页面提交 Profile 与 JSON Merge Patch Preferences，经 BFF→Account API→SQL |
| CSRF/ETag | PASS | 缺 CSRF 返回 403；过期 `If-Match` 返回 412 |
| 普通近期认证与敏感动作 | PASS | Local Mailpit `token_hash`→独立事件 session→proof cookie→Global Delete request 202→Account Close 200 |
| Admin AAL1/MFA/停用恢复 | PASS | AAL1 管理 API 403/MFA_REQUIRED；真实 Admin MFA 后从平台页面 suspend，Consumer 受保护路由 403/ACCOUNT_SUSPENDED，再 restore |
| Browser bundle 凭据扫描 | PASS | Admin/Consumer `.next/static` 未发现 Platform Key、HMAC secret、DB URL、Service Role 或 Supabase secret 标识 |
| 根测试入口 | PASS | `pnpm run test:e2e` 与 `pnpm run test:e2e:t16-r2` 使用同一 runner；当前复跑输出 19 项 PASS |

## 变更范围

- `tests/spikes/e2e/t16-r2-account.mjs`：可复现的 Local fixture、中央/应用进程编排、双上下文浏览器流程、负向验证、bundle 扫描和精确清理。
- `package.json`：`test:e2e` 从占位命令接入 T16-R2 runner，并保留任务专用别名。
- `docs/development/tasks/closeout-01.md`、`docs/development/status.md`：更新 T16-R2 Local 结论并把 T18-L 标为 READY。

## 限制与交接

本证据不代表托管 CI 已运行，也不覆盖 hosted Auth/SMTP/Storage、TLS、生产部署或跨真实域名 Provider 回调。中央不可用、proof 过期、returnTo 及更广泛多用户业务回归仍需在 T18-L/后续 CI 矩阵中持续保留；Local 服务未启动时 runner 应失败而不是静默跳过。T18-L 现在可以按候选 commit 重跑 G1/G2-L 收口。

## 2026-09-10 当前代码与 FE-V Admin/Files/Settings 复跑（历史增量）

- 被验证代码基线：`main@94631ff`；该提交包含 Platform Workspace 在 MFA step-up 与状态刷新期间保持上下文的修复及 Files/Settings 错误/恢复矩阵，FE-D02 批次边界仍以 `54ff797` 为产品基线；最新 runner 扩展提交同为 `94631ff`。
- 命令：`pnpm --filter admin build`；`pnpm run test:e2e:t16-r2`。
- 结果：Admin production build 与 TypeScript 通过；浏览器 runner 退出码 `0`，共 19 项均为 `PASS`。新增矩阵验证 429/503/409 主提示为可操作中文、技术 code 仅保留在可展开技术详情中；Files 验证策略先返回、文件列表延迟、各状态按钮边界、下载存储错误/MFA step-up、Policy MFA step-up 后 409 冲突，以及 DELETE 网络中断后权威检查不自动重发；Settings 验证 Origins/Platform Keys 列表 503 后显式重试、Origin 无效 URL 校验/创建后刷新、平台设置 409 与网络 unknown 不自动重发，以及 Platform Key secret 只展示一次、确认部署后撤销。
- 复跑前本地 Supabase 曾因未加载仓库邮件模板导致 Mailpit 缺少 `token_hash`；按 Supabase 本地配置要求 stop/start 后恢复，当前复跑已通过。该处理仅影响 Local 环境，不升级 Hosted/Staging 结论。
- 仍未关闭：FE-V 全状态故障注入、迟到响应、完整 409/412/429/503/202 全矩阵、Settings 其余状态恢复、Hosted/Staging/生产及正式发布门槛；本次新增 Local 证据不等价于全部资源正向矩阵。

## 2026-09-10 FE-V 资源竞态与完整错误矩阵复跑

- 被验证代码基线：`main@c9c19f4`；GitHub 远端 `origin/main` 已核对为完整 SHA `c9c19f45d97c2f9b7265c62861ed160a2e987dd5`。本提交未修改公共 OpenAPI、数据库迁移或业务领域合同。
- 命令：`pnpm --filter admin typecheck`、`pnpm --filter admin build`、`pnpm lint`、目标文件 `oxfmt --check`、`node --check tests/spikes/e2e/t16-r2-account.mjs`、`pnpm run test:e2e:t16-r2`。
- 静态结果：Admin typecheck/build、根 lint、目标格式、JS 语法检查均退出码 0；impeccable detector 对变更 UI 文件返回 `[]`。Admin MFA 页面已将二维码 `<img>` 替换为 Next Image，lint warning 清零。
- 浏览器结果：T16-R2 Local runner 退出码 0，汇总 20 项 PASS。新增 `adminResourceFailureMatrix` 覆盖目录 400/403/404/409/412/428/429/500/503；Plans 429/412；Accounts 503/202；Operations 503/429；Subscriptions 412/503；Files 列表 503/删除 202；Policy 412；Platform Settings 412；Origins 503 重试及 late/fresh 乱序响应，新响应最终保留。原有 Consumer Close/Delete、网络 unknown、Admin suspend/restore、Files 状态/下载/MFA 等场景继续通过。
- 状态收敛：刷新失败均保留已知资源；202 只展示 accepted/受理并等待权威刷新；关闭/重开流程按服务端状态变化验证；unknown outcome 的状态检查不重发原 mutation。
- 失败与复测：本轮第一次 E2E 因平台目录页面实际使用统一“请检查网络或服务状态后重试”文案，而测试误期待资源列表文案而失败（退出码 1，`exerciseAdminErrorCopyMatrix`）；依据实际页面 contract 修正测试期望后再次运行，退出码 0，20 项 PASS。失败记录保留，不视为产品通过。
- 未覆盖：FE-V01～16 全量正向高风险流程、390px/完整键盘焦点矩阵、Hosted/Staging/生产、真实 Storage 迟到写入和正式发布；上述仍保持 PARTIAL、NOT_RUN 或 BLOCKED。
