# M4-09：关闭保留清理与 Global Purge 证据

日期：2026-09-09  
任务：M4-09  
分支：`task/M4-08-file-ui`  
依赖：M4-05、M3-R1、T12-R2（Local 依赖可用）

## 当前结论

本任务为 **PARTIAL（Local）**。已交付 Admin 近期 MFA 批准 Global Delete、任务列表/详情/受控重试、Admin 文件受控删除、Close 30 天候选与 lease/fence 清理，以及带 lease/fence 的 Global Delete checkpoint 领域函数和 maintenance claim/step 边界。数据库状态不会把 Auth、Storage、备份屏障的未确认结果写成完成。真实 Auth session/user 删除、Storage 对象逐项清除和外部备份屏障适配仍未完成，因此不宣称完整 M4-09 PASS。

## 已实际验证

| 验证 | 结果 | 证据 |
|---|---|---|
| M4-09 函数、SECURITY DEFINER、角色最小权限、租约 fence/有序 checkpoint/backup barrier 断言 | PASS（40/40） | `pnpm test:db`；20 个 pgTAP 文件、371 个断言全部通过 |
| Account API Admin deletion-jobs start/list、Admin file DELETE | PASS（19/19） | Deno `supabase/functions/account-api/index.test.ts` |
| maintenance Global Delete claim/step 边界、旧 lease 输入路径 | PASS（4/4） | `pnpm run test:maintenance` |
| Close 30 天候选、账户 lease/fence、tombstone cleanup 与 backup barrier guard | PASS（5/5） | `pnpm run test:maintenance`；`m4_09_retention_cleanup.sql` 10/10、`m4_09_backup_barrier_guard.sql` 7/7 |
| Consumer/Admin 构建、合同、文档和格式 | PASS | 两个 Next build、`pnpm run contracts:check`、`pnpm run docs:check`、`pnpm run format:check` |
| 本地删除任务页面错误/空状态与可访问控件 | PASS（Local） | `http://localhost:3101/admin/deletion-jobs`；CUA AX tree 核对 |

## 已交付边界

- `admin_deletion_job_start` 将 `pending_admin` 请求批准为 job，并在同一事务设置 `identity_lifecycle=deleting`；重复请求受 Admin idempotency 保护。
- `admin_deletion_job_retry` 仅允许 `blocked/retry` 且要求当前 session 的近期 MFA proof。
- `deletion_job_claim`/`deletion_job_step` 通过 `job_leases`、fence、严格 checkpoint 顺序防止旧 worker 覆盖新状态。
- `admin_file_delete_request` 复用文件预算/Storage cleanup 状态语义，202 仅表示进入 `deleting` 或无 Storage 写入时安全释放。

## 未完成与交接

仍需继续：Global Delete 的真实 session revoke/Auth delete 与 Storage remove adapter；删除前后的 backup barrier；以及每个外部副作用的超时、unknown、重启和多 worker 故障测试。当前这些均不得标记为 PASS，也不应把 Local provider 边界升级为托管或生产证据。
