# M4-03：策略、配额预约和接收状态 SQL 证据

日期：2026-09-08  
任务：M4-03  
分支：`task/M4-03-file-intent`  
依赖：M4-02（PASS，Local）

## 当前结论

本任务为 **PASS（Local）**。已实现受控 Admin 文件策略更新、普通账户 file intent 预约、幂等重放/冲突、接收 lease/fencing claim，以及 prepare-store 的有界实际大小/hash记录和唯一未结算 write attempt。所有状态/预算变更先锁账户，再锁目标文件；Storage 不在这些 SQL 事务内调用。

本任务不宣称 HTTP body 有界读取、真实 Storage adapter、finalize、删除/对账 worker、Replace 原子切换或 G4-L/G4-S 完成；这些由 M4-04～M4-11 承接。

## 实际验证

| 验证 | 结果 | 证据 |
|---|---|---|
| Local migration apply | PASS | 固定 CLI 应用 `20260908151052_m4_03_file_policy_intent.sql` |
| 结构/权限/函数回归 | PASS（21/21） | `supabase/tests/m4_03_file_policy_intent.sql`；SECURITY DEFINER、固定 search_path、最小 executor grants、lease/调度索引和 Audit 入口 |
| 全量 Local pgTAP | PASS（12 files / 233 tests） | `pnpm run test:db`；M4-02、M4-03与既有M0～M3测试全部通过 |
| 真实 Local SQL 流程 | PASS | `pnpm run test:sql:m4-03-file-intent`；预约/占用、同/异请求幂等、claim/fence、prepare/in-flight、唯一 attempt、超配额和三条 Audit 全部 PASS |
| 静态检查 | PASS | `pnpm exec oxfmt --check`、`pnpm run lint`、`pnpm run typecheck`、`pnpm run docs:check` |

## 变更范围

- `supabase/migrations/20260908151052_m4_03_file_policy_intent.sql`：策略更新、intent 预约、receive claim、prepare-store 和 lease_owner 字段。
- `supabase/tests/m4_03_file_policy_intent.sql`、`tests/spikes/sql/m4-03-file-intent.mjs`：pgTAP 权限/结构与真实 Local Auth session/SQL 事务回归。
- `package.json`：新增 `test:sql:m4-03-file-intent` 入口。
- `docs/development/tasks/batch-03.md`、`docs/development/status.md`、`docs/development/modules/M4-files-jobs.md`：更新 M4-03 Local 结论和 M4-04 READY。

## 交接

M4-04 应复用这些函数和 `platform_config_files` 的 reserved/status/write_outcome 语义，在读取 body 前取得接收名额，接收完整字节后调用 prepare-store，再经 server-only immutable Storage adapter；不得新增浏览器 complete/signed-upload 路径，不得以 HTTP 超时推断 unknown 写入失败。
