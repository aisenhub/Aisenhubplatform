# T12-R2：普通近期认证与管理员 MFA 闭环证据

日期：2026-09-08  
任务：T12-R2  
分支：`task/M3-R1-fixture-regression`（承接 `task/T12-R2-mfa-reauth`）

## 当前结论

本任务为 **PARTIAL**。

已交付 Admin 的真实 MFA 消费链：登录后进入 MFA 页面；服务端用当前 HttpOnly session 恢复请求级 Auth client，列出已验证 TOTP 因子，调用官方 `auth.mfa.challengeAndVerify`，再使用升级后的 session 调用中央已登记的 `/admin/api/v1/auth/recent-proof`。中央成功返回并校验 UUID proof 后，才写入绑定 Admin 会话的 HttpOnly、5 分钟 proof cookie；proof 不接受浏览器提交的任意值。

中央 Account API 现对除 recent-proof 签发路由外的所有 Admin 路由统一拒绝 AAL1。敏感写操作仍由 `private.admin_step_up_valid` 再次实时检查 user/session/proof，refresh 不延长 proof 窗口，logout 清除本地 proof 并撤销 Auth 会话。

普通用户 reauthentication 的 SDK 适配、服务端 issuer、Account OpenAPI/DTO、account-server 方法和 Consumer BFF 已补齐。issuer 只接受当前 session 的 Supabase Auth reauthentication OTP，通过 Auth HTTP 验证成功后调用受控 SQL 函数，按 user/session 写入 5 分钟 proof；不使用 JWT `iat`、AAL2 或前端状态替代认证事件。Close 与 Global Delete request 仍待 T16-R1 接入该 proof 消费链。

## 实际验证

| 验证 | 结果 | 证据边界 |
|---|---|---|
| Auth 包 MFA/reauth helper 单测 | PASS（7/7） | 验证 request-scoped 委托参数及 `type: reauthentication` |
| Account/domain SDK与普通 proof issuer | PASS | account-server 单测 9/9；Account API Deno 10/10，包含 reauthentication 成功/拒绝和 Admin AAL1 负向 |
| 普通 proof SQL 权限/函数回归 | PASS（4/4） | `supabase test db --local supabase/tests/t12_recent_auth_proof.sql`；函数存在、account_executor授权、Admin拒绝、固定 search_path |
| Account OpenAPI/合同检查 | PASS | Account 18 operations、Admin 32 operations；引用、鉴权、no-store及样例检查通过 |
| Admin app typecheck | PASS | 新增 factors/verify 路由和 MFA 页面通过 TypeScript |
| Admin production build | PASS | `/admin/mfa`、`/api/auth/mfa/factors`、`/api/auth/mfa/verify` 进入产物 |
| Consumer BFF typecheck/build | PASS | reauth start/verify 与 Account API catch-all route 通过 TypeScript 和 production build |
| Account API Admin AAL1 负向 | PASS（Deno 10/10） | Admin recent-proof 保持专用 AAL2 路径；其他 Admin 路由统一 `MFA_REQUIRED` |
| 格式与 diff 检查 | PASS | Oxfmt、`git diff --check` |
| 浏览器登录→真实 TOTP→中央 proof→敏感写 | NOT_RUN | 需要可复现的真实 Local 浏览器/应用 HTTP fixture；未把静态构建或单测当作 E2E |
| 普通用户真实 Auth 邮件 OTP→BFF proof | NOT_RUN | issuer 已实现；尚未用 Local Auth 邮件捕获器/真实应用浏览器验证 OTP、跨 session、过期、退出失效 |

## 变更范围

- `packages/account-auth-nextjs`：request-scoped session 恢复、MFA factor 查询/验证和 reauthentication helper。
- `apps/admin`：MFA 因子读取与验证 BFF、登录后的 MFA 页面；proof 只由中央成功响应触发写入。
- `apps/template-preview`：普通用户 reauthentication start/verify BFF 与 recent-proof API adapter。
- `supabase/functions/account-api`：Admin AAL2 入口统一门禁、普通 reauthentication issuer 及负向回归。
- `supabase/migrations/20260908073740_t12_user_recent_auth_proof_issue.sql`：新增受控普通 proof issuer；通过固定 CLI 生成并在 Local 应用。
- `supabase/tests/t12_recent_auth_proof.sql`：普通 issuer 的 pgTAP 权限/search_path 回归。

## 交接

T12 仍不能关闭，T16-R1 依赖 T12-R2 完成而继续 WAITING。下一步是用真实 Local Auth 邮件捕获器和浏览器补齐普通 reauthentication、Admin MFA、跨 session/退出失效及敏感写生命周期回归；在此之前不得将 G2-L 或 V-AUTH-03 标为 PASS。
