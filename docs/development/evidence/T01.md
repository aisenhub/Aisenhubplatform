# T01 环境、上游与导入清单核验

日期：2026-09-07  
任务：T01  
分支：`task/T01-source-inventory`  
依赖：无  
基线：`f981ca533db67e543bbe3f6d88c337b78c0199d4`

## 结果摘要

T01 验收：PASS。

固定上游 commit 已取得、许可证/manifest/lock/workspace/安装脚本和 Starter 数据库边界已审查，导入清单足以交给 T02，不需要重新选择 Starter。Supabase CLI 与 Deno 当前缺失已如实记录；这不阻塞 T01，但 Deno 必须在 T03 双运行时探针前补齐。

本任务没有导入上游代码、安装依赖、执行上游安装脚本、执行迁移、启动 Supabase 或运行应用构建。因此 `SP-SOURCE` 的构建部分、`V-BASE-01/02` 及 G0-L 仍为 `NOT_RUN`，不能由本文宣称通过。

## 实际环境

| 工具 | 实际结果 | 状态 |
|---|---|---|
| Node | `D:\APP\Base\Nodejs\node.exe`，`v24.19.0` | PASS |
| pnpm | `C:\Users\S1786\AppData\Roaming\npm\pnpm.cmd`，`11.24.0` | PASS；与上游目标 11.18.0 有差异 |
| npm | `11.17.0` | 记录，不作为项目包管理器 |
| Docker | `D:\APP\Base\DockerDesktop\resources\bin\docker.exe`，客户端/服务端 `29.7.2` | PASS；Docker Desktop 服务可用 |
| Git | `2.55.0.windows.5` | PASS |
| Supabase CLI | PATH、`D:\APP\Codex`、`D:\APP\Base`、常见用户程序目录及 npm bin 目录未发现 | NOT_FOUND |
| Deno | 同上，未发现可执行文件 | NOT_FOUND |

Docker 只读摘要：Server `29.7.2`，OperatingSystem `Docker Desktop`，Containers `12`，Images `16`。本任务没有启动、停止或清理容器。

上游依赖声明要求 Node `>=22.13.0`、`packageManager=pnpm@11.18.0`。本机 Node 满足要求；pnpm 高于目标版本，T02 必须按上游声明和 lockfile 处理，不得静默升级或重写 lock。当前不存在需要安装到 C 盘的工具。若后续需要系统级工具，目标分别为 `D:\APP\Codex\SupabaseCLI\` 和 `D:\APP\Codex\Deno\`；本次未安装。

## 固定上游核验

来源：`https://github.com/makerkit/nextjs-saas-starter-kit-lite`  
固定 commit：`c5cba64391a80620309c4178163dc2df42568d1b`  
许可证：MIT；T02 必须保留 MakerKit 版权声明。  
取得方式：临时目录 clone 后 fetch 固定对象并 detached checkout；checkout 后工作区干净，HEAD 与目标 SHA 一致。

固定 commit 的关键事实：

- 根 `package.json` 的项目包名为 `next-supabase-saas-kit-turbo-lite`，package manager 为 pnpm 11.18.0。
- `pnpm-lock.yaml` 为 lockfile v9；workspace 包范围为 `apps/*`、`packages/**`、`tooling/*`。
- lock 中 Supabase CLI 包解析为 `supabase@2.111.0`，并启用了其构建许可配置。
- catalog 包含 Next 16.3.0、React 19.2.8、TypeScript 7.0.2、Vitest 4.1.10、`@supabase/supabase-js` 2.111.0、`@supabase/ssr` 0.12.4 和 Oxlint/Oxfmt。
- 上游 README 的 Next.js 版本文字与 manifest/catalog 不一致；后续以 manifest、catalog 和 lockfile 为准。

直接查询远端 ref 不返回该 SHA 是因为它不是分支/tag ref；实际 fetch 固定对象与 checkout 已成功，故来源可取得。

## 上游目录与依赖图

上游固定 commit 共 393 个 tracked files，主要目录如下：

| 上游目录 | 处理建议 | 原因/边界 |
|---|---|---|
| `.github`、根 tooling 配置 | 保留后改造 | 复用基础 CI/工具配置，但必须固定 Node/pnpm/CLI，禁止部署和自动 fix |
| `apps/web` | 改名为 `apps/admin`，选择性保留 | 保留 Admin 壳和必要 Auth/SSR 胶水；删除营销页、Demo、Starter 业务和普通产品 Dashboard |
| `apps/e2e` | 改造为 `apps/template-preview` 的测试入口 | 只保留最小 Consumer/Auth/回调/受保护页探针，不保留 Starter 业务链路 |
| `apps/web/supabase` | 移到根 `supabase/` 后重建 | 当前包含旧 config、单个 Starter migration、seed 和邮件模板；不能直接作为本项目生产迁移 |
| `packages/features/accounts` | 不迁移 Starter 账户领域；仅选择性复用 UI 形态 | 其 personal/team account 模型不是 `platform_accounts`，并含个人账户删除和头像 Storage 逻辑 |
| `packages/features/auth` | 选择性保留 Auth UI 参考并改接 M2 合同 | 不复制其业务授权或缓存约定 |
| `packages/supabase` | 拆分/改造为 `account-auth`、`account-auth-nextjs`、`account-server` | 当前含浏览器/服务端 Supabase client、service-role client、Starter generated types 和直接业务访问 |
| `packages/next`、`shared`、`i18n`、`ui` | 选择性保留基础设施 | 保留适用的 Next、类型、国际化和 UI 基础；重命名包并移除 Starter 领域依赖 |
| `tooling/typescript`、`tooling/scripts`、`turbo` | 保留后审查 | 复用工具链；禁止保留自动修复、危险清理和未审查的环境检查副作用 |
| 根 README/许可证 | 保留并重写项目说明 | 不让上游覆盖 AGENTS、docs、Git 历史；保留 MIT 声明 |

依赖关系（仅列 workspace 依赖）：`web → accounts/auth/i18n/next/supabase/tsconfig/ui`；`accounts → i18n/next/shared/supabase/tsconfig/ui`；`auth → shared/supabase/tsconfig/ui`；`i18n → shared/tsconfig`；`next → auth/supabase/tsconfig`；`ui → i18n/shared/tsconfig`。T02 改名和拆包时必须同步 workspace、路径别名、脚本和 E2E 引用。

## Starter 数据库、权限与配置审查

固定 commit 的数据库目录实际位于 `apps/web/supabase`，不是仓库根目录。`config.toml` 暴露 `public`、`storage`、`graphql_public`，默认项目使用 Postgres 15、本地端口 54321/54322/54323，并配置 localhost Auth redirect。T02 只能将本地配置迁至根目录后重新审核，不能把这些设置当作生产配置。

旧 migration `20241219010757_schema.sql` 包含：

- `public.accounts` Starter 顶级账户表及其 RLS、字段保护 trigger 和 Auth 用户 trigger；
- `kit` schema 与 `unaccent` extension；
- 对 `public`、`storage`、`authenticated`、`service_role` 的 GRANT/REVOKE；
- Storage account image bucket/policy；
- `SECURITY DEFINER` 函数和直接关联 `auth.users` 的 Starter 逻辑。

这些内容与本项目的 Platform/Tenant、private SQL、独立 executor、受控 Storage 和共享领域过程边界不相容。T02 不执行、不复制该 migration 到生产迁移目录；T08 依照数据模型重新生成迁移。上游 seed 没有可作为本项目 fixture 的业务数据。

上游跟踪 `.env`、`.env.development`、`.env.production`、`.env.test`，其中包含非空环境项，包含 service-role 变量名；本报告不记录任何值。T02 不复制这些文件，只建立脱敏 `.env.example`，本地 Secret 不入 Git。

## 脚本与 CI 审查

| 位置 | 发现 | T02/T03 处理 |
|---|---|---|
| 根 `preinstall` | 执行 `scripts requirements` | 安装前审查并保留可复现检查 |
| 根 `postinstall` | 执行 `manypkg fix`，会修改工作区文件 | 禁止作为无审查安装副作用；改为明确、受控步骤 |
| 根/workspace `clean` | `git clean -xdf` | 禁止直接继承；改为项目受限清理并明确保护规则 |
| 根 `lint:fix`/`healthcheck` | 含 Oxlint/Oxfmt/manypkg 自动修复 | CI 改为只读 `format:check`/`lint`，自动 fix 仅显式本地命令 |
| `apps/web` `supabase:deploy` | `supabase link` 后执行 `db push` | 删除或改成不具备生产部署权限的检查入口 |
| GitHub workflow | Node `lts/*`、pnpm setup 未固定版本、E2E 中 CLI action 未固定 CLI 版本 | T03 固定 CI 工具链；不向不可信 PR 提供 Secret |
| `allowBuilds` | 允许多个原生构建包和 Supabase CLI 构建 | T02 安装前确认实际依赖范围，保持 lock 一致 |

## 安装与执行边界

官方 Supabase CLI 文档支持项目级依赖并要求锁定版本；本项目优先使用上游 lock 的 `supabase@2.111.0`，通过 `pnpm exec supabase` 调用，不主动安装全局软件。Deno 当前没有已验证版本，T03 前按官方 Windows 分发补齐并记录精确版本；若需要系统安装，只允许放在 `D:\APP\Codex\Deno\`。

本任务实际未执行：`pnpm install`、`pnpm exec supabase --help`、`deno --version`、上游 preinstall/postinstall、build、clean、Supabase start/reset/deploy、migration、CI。上述项目均保持 `NOT_RUN`，不能写成 PASS。

## 可重复检查

以下检查已实际完成：

| 检查 | 结果 |
|---|---|
| Git 分支/工作区核对 | PASS；任务分支从 `f981ca5` 建立，未修改用户已有文件 |
| 固定 commit fetch/checkout | PASS；HEAD 与目标 SHA 一致 |
| 工具路径/版本核对 | PASS；缺失工具明确为 NOT_FOUND |
| Docker 服务只读检查 | PASS；未改变容器状态 |
| manifest、lock、workspace、脚本审查 | PASS |
| Starter migration/权限/环境边界审查 | PASS；未执行 SQL、未读取或记录 Secret 值 |
| clean install/build | NOT_RUN；属于 T02 |

复现固定上游核验时使用：

```text
git clone --no-tags --filter=blob:none https://github.com/makerkit/nextjs-saas-starter-kit-lite.git <临时目录>
git -C <临时目录> fetch --no-tags origin c5cba64391a80620309c4178163dc2df42568d1b
git -C <临时目录> checkout --detach c5cba64391a80620309c4178163dc2df42568d1b
git -C <临时目录> rev-parse HEAD
```

## 后续任务

T02 READY。开始前必须按本清单隔离导入，保留根 `AGENTS.md`、`docs/`、`.git` 历史和 MIT 声明；先禁用危险脚本，再按固定 lock 安装。T02 仍不得实现 M1/M2 业务、执行生产部署或将 Starter `accounts` 表当作本项目账户模型。
