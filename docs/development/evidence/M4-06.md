# M4-06：Replace 原子切换与竞争恢复证据

日期：2026-09-09  
任务：M4-06  
分支：`task/M4-06-replace-switch`  
依赖：M4-05（PASS，Local）

## 当前结论

本任务为 **PASS（Local）**。新对象独立预约和写入完成后，数据库同事务切换新文件为 active、旧文件为 deleting；双份预算在旧对象由 M4-05 remove 确认前持续保留。Replace/Delete 竞争返回 `FILE_BUSY` 并将已确认的新对象交给补偿清理，容量不足映射为 `REPLACEMENT_CAPACITY_REQUIRED`。

## 已实际验证

| 验证 | 结果 | 证据 |
|---|---|---|
| 数据库函数、返回合同、权限和不可变路径约束 | PASS（287/287） | `pnpm run test:db`；15 个 pgTAP 文件全部通过 |
| Replace 原子切换、旧对象 deleting、双份预算 | PASS | `pnpm run test:sql:m4-06-replace-switch`；`atomicSwitch` 为 PASS |
| Replace/Delete 竞争与新对象补偿 | PASS | 同一 SQL 探针；`busyCompensation` 为 PASS |
| 严格容量错误合同 | PASS | 同一 SQL 探针；`capacityGuard` 为 PASS；API 稳定码由单测覆盖 |
| Account API | PASS（16/16） | Deno account-api tests；新增 DELETE、Replace capacity 映射 |
| Workspace与合同回归 | PASS | `pnpm run test:unit`、`pnpm run typecheck`、`pnpm run lint`、`pnpm run format:check`、`pnpm run docs:check`、`pnpm run contracts:check`、`git diff --check` |

## 变更范围

- `supabase/migrations/20260908170431_m4_06_replace_switch.sql`：追加带 `replace_outcome` 的可信 finalize；按账户锁和文件 UUID 顺序锁定，原子切换或进入 FILE_BUSY 补偿。
- `supabase/functions/account-api/index.ts`、`packages/domain/src/contracts/api.ts`、Account SDK/BFF合同：Replace容量稳定错误码和文件删除入口同步。
- `supabase/tests/m4_06_replace_switch.sql`、`tests/spikes/sql/m4-06-replace-switch.mjs`、Account API单测：结构、竞争、容量和错误合同验证。

## 交接与限制

本地 Storage 对象写入/删除分别由 M4-04/M4-05 的实际 Local 探针覆盖；本任务未宣称托管/生产 Replace 证据。M4-07 可继续实现文件列表、状态查询和下载代理。
