# M4-02：持久模型、权限与备份屏障基础证据

日期：2026-09-08  
任务：M4-02  
分支：`task/M4-02-file-persistence`  
依赖：M4-01、T18-L（均已完成 Local 门槛）

## 当前结论

本任务为 **PASS（Local）**。已按 M4-01 合同新增文件策略、配置文件、Storage 写入尝试、备份删除屏障和最小删除墓碑持久模型，补齐同平台/账户复合 FK、Replace 活跃目标唯一约束、未结算写入唯一约束、调度索引、updated_at、RLS 和运行时最小权限。生成的 public 数据库类型同步了策略和配置文件表。

本任务不宣称上传/Storage adapter、配额预约过程、worker、对账、删除执行、Replace 切换、UI 或 G4-L/G4-S 完成；这些由 M4-03～M4-11 承接。

## 实际验证

| 验证 | 结果 | 证据 |
|---|---|---|
| 空库 reset 与迁移升级 | PASS | `pnpm run db:reset` 后固定 CLI 应用完整迁移集，包含 `20260908150020_m4_02_file_persistence.sql` |
| M4-02 结构/RLS/权限 | PASS（28/28） | `supabase/tests/m4_02_file_persistence.sql`；5 张表、强制 RLS、复合 FK、partial unique index、策略/状态字段、domain-owner policy 和运行时直写拒绝 |
| 全量 Local pgTAP | PASS（11 files / 212 tests） | `pnpm run test:db`；既有安全表计数同步为 14 张 private 表，原有测试全部通过 |
| 数据库类型 | PASS | `packages/shared/src/database.types.ts` 增加 `platform_file_policies`、`platform_config_files` 及关系字段 |
| 静态与文档 | PASS | `pnpm run format:check`、`pnpm run lint`、`pnpm run typecheck`、`pnpm run docs:check` |

## 变更范围

- `supabase/migrations/20260908150020_m4_02_file_persistence.sql`：M4-02 新表、约束、索引、RLS 与最小权限。
- `supabase/tests/m4_02_file_persistence.sql`、`supabase/tests/t09_security_helpers.sql`：新增结构负向和 private RLS 数量回归。
- `packages/shared/src/database.types.ts`：同步 public schema 类型。
- `docs/development/tasks/batch-03.md`、`docs/development/status.md`：M4-02 Local 完成、M4-03 READY。

## 交接

M4-03 可在本分支继续实现策略读取、账户锁顺序、配额预约、幂等和 receive 状态 SQL；必须复用本任务的持久表和既有 `private.job_leases`，不能在 HTTP/UI 中重算配额或新增第二套任务设施。托管备份介质和真实 Storage 仍保持未运行状态。
