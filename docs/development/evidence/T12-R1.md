# T12-R1 Auth/SSR adapter与会话范围

日期：2026-09-08  
任务：T12-R1  
模块：M2 身份与平台账户  
分支：`task/T12-R1-auth-ssr`  
代码提交：`aa5089d`（已推送 `origin/task/T12-R1-auth-ssr`）  
依赖：T03固定工具链、T04 Local Auth/注销探针、T11合同

## 结果

实现部分：PASS。Local Auth adapter与两个应用认证路由已接入固定版本的 `@supabase/ssr`/`@supabase/supabase-js`，并通过请求级客户端、统一cookie写入、刷新、PKCE callback、CSRF和安全returnTo路径。

本任务整体：PARTIAL。真实浏览器SSR、OAuth Provider、邮件确认/重置和两个应用路由对Local Auth服务的端到端HTTP回归尚未执行，不能把代码构建等同于完整V-AUTH-04通过。

## 环境与命令

- 环境：Local Windows workspace；Supabase CLI `2.111.0`；Node `24.19.0`；pnpm `11.18.0`；Next `16.3.0`；`@supabase/supabase-js` `2.111.0`；`@supabase/ssr` lockfile固定版本。
- `pnpm --filter @kit/account-auth-nextjs test:unit`：PASS，6/6。
- `pnpm run test:unit`：PASS，6个测试包全部通过。
- `pnpm --filter @kit/account-auth-nextjs typecheck`、两个应用 `typecheck`：PASS。
- `pnpm --filter admin build`、`pnpm --filter template-preview build`：PASS，callback/refresh动态路由均进入产物。
- `pnpm run lint`、`pnpm exec oxfmt --check packages/account-auth-nextjs apps/admin apps/template-preview`、`git diff --check`：PASS。
- `supabase status -o json`获取临时进程环境后运行 `node tests/spikes/auth/local-auth.mjs`：PASS，signup/refresh/MFA/logout流程通过；脱敏观察为Provider原始旧access JWT不会立即失效，应用授权继续依赖服务端 `session_id`校验。

## 用例矩阵

| 用例 | 结果 | 证据边界 |
|---|---|---|
| V-AUTH-01 Local密码认证/刷新 | PASS（Auth探针） | Local Auth真实HTTP；应用路由端到端仍待补 |
| V-AUTH-02 请求隔离、CSRF、returnTo、no-store | PASS（单测/静态） | 请求级client、恶意returnTo拒绝、cookie/response策略已覆盖；浏览器并发待T16-R2 |
| V-AUTH-04 OAuth/邮箱确认/重置/Link | NOT_RUN | X03/真实Provider未提供；PKCE交换适配已实现 |
| V-SDK-01 Server包边界 | PASS（构建/类型） | package导出与应用构建通过；独立消费者安装留M5 |

## 变更范围

- `packages/account-auth-nextjs`新增固定Supabase Browser/Server/request-scoped client、刷新/PKCE/password helper及统一session cookie操作。
- Admin与template-preview新增 `/api/auth/refresh`、`/api/auth/callback`，登录改用request-scoped Supabase SDK；服务端凭据没有进入浏览器包。
- 未修改业务SQL入口、RLS、Storage或生产配置；未写入真实token、cookie、用户数据或环境值。

## 局限与下一步

- `T12-R2`继续补普通用户近期认证、Admin MFA/proof消费者与敏感操作恢复；该项未完成前T12和G2-L不关闭。
- `T16-R2`补真实双入口浏览器流程和可复现API/E2E入口。
- Google、SMTP、托管SSR和Provider行为交给T17-R2；本报告不宣称G2-S或OAuth/邮箱验收通过。
