# M6-02：联合备份与外部墓碑实现证据

日期：2026-09-09  
任务：M6-02  
分支：`task/M4-08-file-ui`  
状态：PARTIAL（Local 隔离模拟 PASS；X04 外部备份目标未确认）

## 本轮交付

- 新增 `file_backup_barrier_begin` / `file_backup_barrier_finish` 数据库受控入口：由 `job_executor` 通过 `job_lease` 的 owner/fencing token 开始、完成或失败恢复集；超时先落 `failed` 再解除物理删除屏障。
- 新增本地可复现的 M6-02 隔离模拟，验证 Local Storage 对象 size/SHA-256、恢复集 manifest、脱敏删除墓碑副本和失败恢复集收口；模拟产物只写入临时 `E:\AppData` 目录并在测试结束清理。
- 未新增外部备份服务、云端墓碑目标或真实凭据；X04 缺失时不把 Local 模拟升级为联合备份通过。

## 已实际验证

| 验证 | 结果 | 证据 |
|---|---|---|
| Local migration / pgTAP | PASS | `pnpm run db:reset`；`pnpm run test:db`：28 files、431 assertions |
| job lease/fence barrier lifecycle | PASS | `pnpm run test:ops:m6-02-local`：`localBarrierContract: PASS` |
| Storage object manifest size/hash | PASS | `objectManifestHashVerification: PASS` |
| External tombstone redaction/isolation | PASS | `externalTombstoneIsolation: PASS` |
| Failed recovery set marks failed and releases barrier | PASS | `failedRecoverySetReleasesBarrier: PASS` |
| X04 independent encrypted target / delivery | NOT_RUN | `externalBackupTarget: NOT_RUN (X04 unavailable)` |

## 限制与下一步

本证据只关闭 M6-02 的 Local 隔离准备，不宣称 V-OPS-01 或真实联合备份完成。需要 X04 独立加密备份目标、凭据托管和告警渠道后，补充 DB 一致快照、对象复制、不可变 manifest、恢复集失败告警及外部墓碑持久化/重应用验证；M6-03/M6-04 仍分别依赖受控恢复目标和容量/告警输入。
