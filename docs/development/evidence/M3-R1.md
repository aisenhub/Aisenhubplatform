# M3-R1 G3 Local证据矩阵收口

日期：2026-09-08  
任务：M3-R1  
模块：M3 权益与兑换  
分支：`task/M3-R1-local-acceptance`  
依赖：M3-01～03 Local交付  
状态：PARTIAL

## 实际验证

使用Local Supabase临时进程环境运行：

- `node tests/spikes/sql/m3-entitlement-ledger.mjs`：PASS。
- 启动 `supabase/functions/account-api/index.ts` 后运行 `node tests/spikes/api/m3-account-api.mjs`：BLOCKED/FAIL，在创建临时Admin fixture时因既有 `private.system_admin` singleton 行触发 PostgreSQL `23505 system_admin_pkey`，尚未进入API断言；现有本地fixture未删除、未覆盖。

SQL探针脱敏结果：

```json
{"defaultFree":"PASS","adminGrant":"PASS","idempotency":"PASS","planConflict":"PASS","pauseResume":"PASS","revoke":"PASS","roleBoundary":"PASS","ledger":"PASS","delivery":"PASS","redemption":"PASS","concurrentRedemption":"PASS","planManagement":"PASS"}
```

## 用例判定

| 用例 | 结果 | 说明 |
|---|---|---|
| V-ENT-01～03 | PASS（SQL Local） | Free/Pro、Plan冲突、暂停/恢复、撤销、Ledger及计划管理已实际运行 |
| V-REDEEM-01～04 | PASS（SQL Local） | 幂等、交付、兑换、并发兑换和权限边界已实际运行 |
| V-DB-03 | PASS（SQL Local） | M3关系/角色边界由探针覆盖 |
| M3 Account API全链路 | NOT_RUN | Admin singleton fixture冲突发生在API断言前 |
| G3 | NOT_RUN | 完整矩阵还缺中央API回归及月末/闰年/UTC、影子重放等逐断言证据 |

## 局限与恢复动作

- 失败用例已保留；不能把“SQL探针通过”扩展为G3整体通过。
- 需要在隔离Local项目或项目所有者确认的测试fixture清理窗口内处理既有singleton，再重跑API探针；不使用生产数据或删除未知用户。
- M3-R1仍需补月末/闰年/UTC边界、影子Projection重放、无订阅多码首次竞争、兑换/Admin Grant竞争、Batch禁用双顺序及HMAC轮换/响应丢失矩阵。
- 未修改已应用迁移、未创建新数据库资源、未写入凭据或用户数据。
