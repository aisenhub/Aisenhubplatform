# M3-R1 G3 Local证据矩阵收口

日期：2026-09-08  
任务：M3-R1  
模块：M3 权益与兑换  
分支：`task/M3-R1-fixture-regression`  
依赖：M3-01～03 Local交付  
状态：PASS（Local）
前置提交：`e8c47c1`、`46451c8`（均已推送并核对 `origin/task/M3-R1-fixture-regression`）

## 实际验证

使用Local Supabase临时进程环境运行：

- `node tests/spikes/sql/m3-entitlement-ledger.mjs`：PASS。
- 原始运行曾在创建临时Admin fixture时因既有 `private.system_admin` singleton 行触发 PostgreSQL `23505 system_admin_pkey`，尚未进入API断言。随后将两个 M3 fixture 改为先读取并在 finally 恢复 singleton；默认仍拒绝已有 singleton，只有显式 `M3_ALLOW_SYSTEM_ADMIN_SWAP=1` 才允许一次隔离 Local 运行，避免误覆盖现有映射。
- `node tests/spikes/api/m3-account-api.mjs`：PASS（候选 Secret 切换前的单 Secret Local HTTP 链路）；Account API executor 在前台运行，覆盖计划、Principal、activate、subscription、Admin plan/batch、交付、redeem、同幂等键响应丢失重试、pause/resume、recent-proof和logout后旧token拒绝。
- 通过固定 Supabase CLI 生成并应用追加迁移 `20260908073108_m3_calendar_interval_fix.sql`；UTC 月末/闰年确定性断言通过：2024-01-31→2024-02-29、2023-01-31→2023-02-28、2024-02-29 加一年→2025-02-28。
- 通过固定 Supabase CLI 生成追加迁移 `20260908103340_m3_dual_secret_redeem.sql`；Local 数据库执行返回 `CREATE/ALTER/REVOKE/GRANT`，目录断言确认候选兑换/交付函数存在且仅对应 executor 可执行。
- `deno test --allow-env --allow-net supabase/functions/account-api/index.test.ts`：PASS，9/9；新增断言确认当前/上一兑换 Secret 以候选数组一次性提交，避免错误版本先占用幂等键。
- `tests/spikes/api/post-commit-drop-proxy.mjs` 已加入真实 HTTP 连接销毁注入：仅在上游成功提交兑换后丢弃首次响应，后续同幂等键请求正常转发；两个脚本均通过 `node --check`。
- 使用 current/previous redemption Secret（版本 2/1）启动临时 Deno Account API，并经真实 Local Auth、transaction pooler 和 HTTP 响应丢失代理运行 `M3_ROTATION_E2E=1 M3_POST_COMMIT_RESPONSE_LOSS=1 node tests/spikes/api/m3-account-api.mjs`：PASS；`dualSecretOldCode`、`postCommitResponseLoss` 及其余全链路字段均为 PASS。

SQL探针脱敏结果：

```json
{"defaultFree":"PASS","adminGrant":"PASS","idempotency":"PASS","planConflict":"PASS","pauseResume":"PASS","revoke":"PASS","roleBoundary":"PASS","ledger":"PASS","delivery":"PASS","redemption":"PASS","concurrentRedemption":"PASS","shadowProjectionReplay":"PASS","emptyAccountMultiCodeRace":"PASS","redemptionAdminGrantRace":"PASS","batchDisableOrders":"PASS","hmacVersionOverlap":"PASS","lostDeliveryResponseRetry":"PASS","planManagement":"PASS"}
```

## 用例判定

| 用例 | 结果 | 说明 |
|---|---|---|
| V-ENT-01～03 | PASS（SQL Local子集） | Free/Pro、Plan冲突、暂停/恢复、撤销、Ledger及计划管理已实际运行；UTC月末/闰年边界已补确定性测试 |
| V-REDEEM-01～04 | PASS（SQL Local） | 幂等、交付、兑换、并发兑换和权限边界已实际运行 |
| V-DB-03 | PASS（SQL Local） | M3关系/角色边界由探针覆盖 |
| M3 Account API全链路 | PASS（Local） | 临时Deno Account API与真实Local Auth/DB HTTP链路通过；包含Admin recent-proof、同幂等键HTTP重试、旧 Secret 兑换和logout后旧token拒绝 |
| G3 | PASS（Local） | SQL/API矩阵、旧/当前HMAC版本重叠、候选 Secret adapter、旧 Secret 端到端兑换/交付及真实提交后响应丢失重试均已通过；不扩展为托管或浏览器验收 |

## 局限与恢复动作

- 失败用例已保留；本证据只将 G3 Local SQL/API 领域收口为 PASS，不将其扩展为托管、浏览器或生产验收。
- fixture singleton冲突已通过显式、可恢复的 Local 隔离开关解决；默认安全拒绝，不能在共享或生产环境设置该开关。追加迁移只修改日历加法函数与调用方，未修改已应用迁移文件。
- M3-R1的 API 双 Secret 轮换实现、候选数据库函数、权限边界、旧 Secret 端到端 Local HTTP fixture 和提交后响应丢失注入均已完成；本结论仅覆盖 Local SQL/API 领域证据，不能扩展为托管运维、浏览器体验或生产验收。
- 未修改已应用迁移；仅追加固定版本 CLI 生成的迁移。回归产生的临时平台、批次、兑换码和 `m3-api-*` 用户已清理，未写入真实凭据或用户数据。
