# M3-R1 G3 Local证据矩阵收口

日期：2026-09-08  
任务：M3-R1  
模块：M3 权益与兑换  
分支：`task/M3-R1-fixture-regression`  
依赖：M3-01～03 Local交付  
状态：PARTIAL
前置提交：`10d8bf3`（已推送并核对 `origin/task/M3-R1-local-acceptance`）

## 实际验证

使用Local Supabase临时进程环境运行：

- `node tests/spikes/sql/m3-entitlement-ledger.mjs`：PASS。
- 原始运行曾在创建临时Admin fixture时因既有 `private.system_admin` singleton 行触发 PostgreSQL `23505 system_admin_pkey`，尚未进入API断言。随后将两个 M3 fixture 改为先读取并在 finally 恢复 singleton；默认仍拒绝已有 singleton，只有显式 `M3_ALLOW_SYSTEM_ADMIN_SWAP=1` 才允许一次隔离 Local 运行，避免误覆盖现有映射。
- `node tests/spikes/api/m3-account-api.mjs`：PASS；Account API executor 在前台运行，覆盖计划、Principal、activate、subscription、Admin plan/batch、交付、redeem、pause/resume、recent-proof和logout后旧token拒绝。

SQL探针脱敏结果：

```json
{"defaultFree":"PASS","adminGrant":"PASS","idempotency":"PASS","planConflict":"PASS","pauseResume":"PASS","revoke":"PASS","roleBoundary":"PASS","ledger":"PASS","delivery":"PASS","redemption":"PASS","concurrentRedemption":"PASS","planManagement":"PASS"}
```

## 用例判定

| 用例 | 结果 | 说明 |
|---|---|---|
| V-ENT-01～03 | PASS（SQL Local） | Free/Pro、Plan冲突、暂停/恢复、撤销、Ledger及计划管理已实际运行 |
| V-REDEEM-01～04 | PASS（SQL Local） | 幂等、交付、兑换、并发兑换和权限边界已实际运行 |
| V-DB-03 | PASS（SQL Local） | M3关系/角色边界由探针覆盖 |
| M3 Account API全链路 | PASS（Local） | 临时Deno Account API与真实Local Auth/DB HTTP链路通过；包含Admin recent-proof和logout后旧token拒绝 |
| G3 | PARTIAL | 中央API基线已通过；仍缺月末/闰年/UTC、影子Projection重放、无订阅多码首次竞争、兑换/Admin Grant竞争、Batch禁用双顺序及HMAC轮换/响应丢失逐断言 |

## 局限与恢复动作

- 失败用例已保留；不能把“SQL探针通过”扩展为G3整体通过。
- fixture singleton冲突已通过显式、可恢复的 Local 隔离开关解决；默认安全拒绝，不能在共享或生产环境设置该开关。
- M3-R1仍需补月末/闰年/UTC边界、影子Projection重放、无订阅多码首次竞争、兑换/Admin Grant竞争、Batch禁用双顺序及HMAC轮换/响应丢失矩阵。
- 未修改已应用迁移、未创建新数据库资源、未写入凭据或用户数据。
