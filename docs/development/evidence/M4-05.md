# M4-05：删除、过期、对账与持久 worker 证据

日期：2026-09-09  
任务：M4-05  
分支：`task/M4-05-file-cleanup`  
依赖：M4-04（PASS，Local）

## 当前结论

本任务为 **PASS（Local）**。删除请求、无写入过期、Storage 删除后的确认释放、unknown/in-flight 保留预算与重试、租约 fencing、备份屏障、对账告警，以及独立 job token 的 maintenance worker 均已通过本地实际验证。

## 已实际验证

| 验证 | 结果 | 证据 |
|---|---|---|
| 数据库结构、RLS、角色与函数合同 | PASS（278/278） | `pnpm run test:db`；14 个 pgTAP 文件全部通过 |
| 删除/过期/结算/重试/旧 fence/备份屏障/对账 | PASS | `pnpm run test:sql:m4-05-file-cleanup`；5 组结果全部为 PASS |
| Account API 既有回归 | PASS（15/15） | `deno test --allow-env --allow-net --allow-read --allow-import supabase/functions/account-api/index.test.ts`；新增 DELETE 202/幂等边界 |
| Consumer BFF | PASS（8 tests） | `pnpm --filter template-preview exec vitest run 'app/api/v1/[...path]/route.test.ts'`；新增 DELETE 代理验证 |
| maintenance worker 单测 | PASS（3/3） | `pnpm run test:maintenance`；job token、固定路由、候选批处理与 Storage/DB 事务边界通过 |
| 实际 Local worker→Storage→DB 流程 | PASS | `pnpm run test:api:m4-05-maintenance`；实际 Storage remove、状态删除和预算释放通过 |
| 上传与静态质量回归 | PASS | `pnpm run test:upload`、`pnpm run typecheck`、`pnpm run lint`、`pnpm run format:check`、`pnpm run docs:check`、`pnpm run contracts:check`、`git diff --check` |

## 变更范围

- `supabase/migrations/20260908161353_m4_05_file_cleanup.sql`：删除请求、过期、cleanup claim/finish、重试退避、备份屏障阻断和对账告警；复用 `private.job_leases`，unknown 不释放预算。
- `supabase/functions/maintenance/index.ts`、`schedule.json`：独立 job token、固定候选批处理/cleanup/reconcile 路由和每分钟/每小时受控调度配置；DB claim/finish 为短事务，Storage remove 在事务外。
- `supabase/functions/account-api/index.ts`、`packages/account-server/src/index.ts`、Consumer BFF 与合同：DELETE 202、幂等 key 和现有文件 DTO。
- `supabase/tests/m4_05_file_cleanup.sql`、maintenance 单测及 Local SQL/API probes：结构、权限、故障恢复与实际 Storage 验证。

## 交接与限制

job token、Storage secret 未进入仓库或输出。上述为 Local 证据，不升级为托管/生产验收；M4-06 可在此基础上实现 Replace 原子切换与竞争恢复。
