# M5-01 资源、包与模板兼容矩阵

日期：2026-09-08  
任务：M5-01  
模块：M5 SDK/Admin/Registry成品  
分支：`task/M5-01-compatibility-matrix`  
依赖：无  
状态：DONE（盘点规格；实现产物仍未创建）

## 交付

新增 `docs/development/m5-compatibility-matrix.md`，基于当前工作树实际代码和冻结OpenAPI盘点平台/Origin/Key/账户、Plan/订阅/批次/Code、文件/策略/删除任务、审计和Auth模板的领域/HTTP/SDK/UI覆盖；明确三个account包的当前exports、固定依赖、browser/server边界、Registry缺口、X05发布条件及M5-02～06承接。

当前事实：Account OpenAPI 17个操作、Admin OpenAPI 32个操作静态检查通过；包仍为private `0.1.0`，不存在 `registry/`；template-preview不是生产部署物；M4字段以M4-01冻结合同为准；没有声称npm/Registry可安装或G5通过。

## 验证

- `pnpm run docs:check`：PASS。
- `pnpm run contracts:check`：PASS，Account 17、Admin 32操作。
- `git diff --check`：PASS。
- Oxfmt：NOT_RUN，Markdown被项目配置排除。
- 未运行应用、数据库、浏览器、Registry安装或Staging/生产验证；这些仍为NOT_RUN。

## 交接

本任务只增加兼容矩阵、路线状态和脱敏证据，不改业务代码、SQL或云端资源。后续实现按M5-02 SDK打包、M5-03 Admin成品、M5-04 Registry/模板和M5-05全新Consumer验收推进；M5-06仍需X05及明确发布授权。
