# T10 数据库负向与事务设施验收证据

更新日期：2026-09-07。任务分支：`task/T10-db-acceptance`。

## 结论

T10 Local 验收通过。测试基于 T08/T09 迁移，未添加新的生产表或业务领域函数；所有失败用例保留在测试中，未放宽权限策略。

## 交付

- `supabase/tests/t10_role_negative.sql`：角色权限快照与运行入口授权断言。
- `tests/spikes/txn/t10-acceptance.mjs`：三平台三用户 fixture、跨租户 FK、Admin scope、Profile 乐观版本、回滚、expired lease fence 和 account/admin/job 角色负向行为。
- T10 证据与状态/任务交接同步更新。

## 实际验证

| 验证 | 结果 | 证据 |
|---|---|---|
| V-DB-02/04 角色权限回归 | PASS | `supabase test db --local`，3 files / 59 tests；T10 fixture 检查 account/admin/job 直接表权限与受控 lease EXECUTE |
| V-TXN-01 Admin scope | PASS | 同一个 Admin/operation/key 在 `platform:<uuid>` 与 `global` scope 可区分，不依赖 NULL 唯一键 |
| V-DB-03 跨租户复合 FK | PASS | Platform A + Platform B account 的 audit 关系返回 SQLSTATE `23503`，无审计副作用 |
| V-DB-04 Profile 版本竞争 | PASS | 两条实际连接先后提交；版本 1 的首个更新到 2，陈旧版本更新影响 0 行 |
| V-TXN-02 故障回滚 | PASS | 注入异常后 pending idempotency 行不存在 |
| V-JOB-01 expired fence | PASS | lease 过期后另一 worker 可 claim，fencing token 从 1 递增到 2 |
| 真实角色负向 | PASS | account/admin/job 角色在 Local 临时 membership 下执行；直接表/helper 越权拒绝，job 仅能调用 lease wrapper |

行为探针通过 Local Auth HTTP 创建三个临时身份，建立 A/U1、B/U1、A/U2、C/U3 账户组合；退出前删除 Auth 用户、平台账户、平台和辅助测试行。凭据只在进程环境中使用，未写入仓库或输出。

## 未完成与交接

- T10 未执行 Staging/Production、真实 TLS pooler、OAuth/SMTP 或完整 G1 性能/恢复矩阵。
- V-AUTH-03 近期 proof 仍因 T04 已签发 access JWT 的 logout 失效边界 BLOCKED；不能将 `admin_step_up` 表存在宣称为 proof 生命周期完成。
- 下一项满足依赖的任务是 T11：基于当前 schema、函数、权限和错误事实冻结 OpenAPI、Context 与错误合同。
