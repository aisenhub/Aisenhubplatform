# T11 OpenAPI、Context与错误合同冻结证据

更新日期：2026-09-08。任务分支：`task/T11-contract-freeze`。

## 结论

T11合同冻结通过。Account合同覆盖API专题列出的17个方法/路径组合，Admin合同覆盖规划中的平台、账户动作、Key、Plan、兑换批次、Subscription、文件、审计和删除任务资源。未实现操作均标记为`contract-only`，没有新增假成功handler。

## 交付

- `docs/contracts/account.openapi.json`：Account OpenAPI 3.1，包含M2/M3/M4范围、鉴权、DTO、错误、ETag、分页、no-store和二进制上传/下载。
- `docs/contracts/admin.openapi.json`：Admin资源动作合同，敏感操作标记服务端step-up要求，不提供任意SQL或Projection更新。
- `packages/domain/src/contracts/api.ts`：稳定错误码、响应包络、分页、权益/文件DTO和Account/Admin/Job SQL context映射。
- `tooling/scripts/src/openapi-check.mjs`与`contracts:check`：引用、operationId、鉴权、样例往返、NULL权益语义、binary/no-store回归检查，并接入CI。

## 实际验证

| 验证 | 结果 | 证据 |
|---|---|---|
| V-SDK-01合同子集 | PASS | `node tooling/scripts/src/openapi-check.mjs`：Account 17 operations、Admin 31 operations，引用与operationId校验通过 |
| schema引用与样例往返 | PASS | OpenAPI本地JSON Pointer引用解析；none权益保留`plan=null`、时间字段`null`和空features |
| 二进制/缓存边界 | PASS | upload使用`application/octet-stream`+`binary`，download保留binary响应，private/public响应声明`Cache-Control: no-store` |
| 共享类型 | PASS | `turbo typecheck`；domain合同单测3/3 |
| 文档/格式/静态检查 | PASS | docs-check、oxfmt check、oxlint |

## 未完成与交接

- 运行时路由、SDK生成包、真实Admin/用户鉴权仍由T12及后续任务实现；本次没有把合同状态写成运行时完成。
- 普通用户Close与Global Delete的近期认证仍受T04服务端session-bound proof阻塞；OpenAPI仅明确`x-requires-recent-auth`和错误合同，未伪造proof。
- 下一项满足依赖的任务是T12；M3权益/兑换实现仍须按后续任务依赖推进。
