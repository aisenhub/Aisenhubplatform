# T08 核心平台与账户迁移证据

更新日期：2026-09-08。任务分支：`task/T08-core-migrations`。

## 结论

T08 Local 验收通过。使用固定项目级 Supabase CLI `2.111.0` 生成迁移文件 `20260907100355_core_platform_accounts.sql`，只覆盖 T08 规定的核心表；没有修改已应用生产迁移，也没有连接远端项目。

## 交付

- `public.platforms`、`platform_accounts`、`platform_profiles`、`platform_preferences`、`platform_auth_origins`、`plans`。
- `private` schema 与内部 `updated_at` trigger helper。
- `private.account_context`、`private.admin_context`、`private.job_context` 受控上下文类型；具体授权函数留给后续任务。
- 平台账户归属、受控匿名化墓碑、JSON object、状态和默认 Free 同平台复合外键约束。
- Profile/Preferences `row_version bigint not null default 1 check (> 0)`。
- 核心表 RLS + FORCE RLS，撤销 `public`、`anon`、`authenticated` 的表权限；后续领域函数交付前不开放运行时 CRUD。
- Local schema 生成的 TypeScript 类型：`packages/shared/src/database.types.ts`。
- pgTAP fixture：`supabase/tests/t08_core_platform_accounts.sql`。

## 实际验证

| 验证 | 结果 | 证据 |
|---|---|---|
| V-DB-01 核心子集：空库 reset | PASS | `supabase db reset --local --no-seed --yes` 连续两次成功，均应用 `20260907100355` |
| V-DB-01 升级基线 | PASS | `supabase migration list --local` 返回 local/remote `20260907100355` |
| V-DB-03 核心子集：同平台/跨平台 Plan | PASS | 默认 Free 可绑定同平台 Free；跨平台 Plan 与 paid Plan 均返回 SQLSTATE `23503` |
| V-DB-04 核心子集：墓碑/版本/触发器 | PASS | 合法 closed tombstone 成功；非法 NULL 身份组合返回 `23514`；两个 row_version 默认 `1`；updated_at 自动前进 |
| V-DB-04 默认拒绝 | PASS | `anon` 读取核心表返回 `42501` |
| pgTAP 核心 fixture | PASS | `supabase test db --local`，24/24 |
| 生成类型/质量检查 | PASS | `supabase gen types typescript --local --schema public` 对照生成；oxfmt、oxlint、shared tsc、全工作区 typecheck、docs-check 均通过 |

探针 `tests/spikes/sql/core-model.mjs` 使用虚构 UUID/代码；脚本 finally 逐项删除测试行，不输出数据库凭据。数据库测试只使用本机 Local Supabase，未执行 Staging/Production 或真实业务函数权限矩阵。

## 未完成与交接

- T08 不包含 M1 辅助表、角色/内部 helper、Grant/Billing、业务更新函数或 M3 事件表；这些不能从本证据推断为已实现。
- T04 的近期 proof 阻塞保持不变；T09 相关 proof 子项需遵守 T04 证据中的 session-bound 约束。
- 下一项满足依赖的任务是 T09，但其依赖表中 T04 仍为 BLOCKED，应只实现独立安全设施并如实标记受阻子项。
